# Live Task Tracker

Status icons: 🔴 blocking · 🟡 ready · ⚪ not started · 🤔 needs founder decision · ✅ done, verified.
[founder-led] = founder's own action, no engineering needed.

## Closed, verified
- #202/#203/#204/#205/#206/#199 — 6 independent small mobile-polish fixes, `apps/web` only — 2026-09-25 (commit `0701bf8`). **#202:** `events/[id]`'s CommodityChip vs. live price-move sentence had no label distinguishing the static predicted-direction chip from the currently-measured price move — added `label="Predicted"` (new optional `CommodityChip` prop) and a "Since signal:" prefix on the price subtext in `MarketImpactAssessment.tsx`; neither underlying value changed. **#203:** `/alerts`' Feed/Watchlist/Lab row looked like an in-page tab switcher (pill styling, no active state) but actually navigated to `/dashboard`/`/watchlist`/`/backtesting` — restyled as plain breadcrumb-style text links (`/` separators, underline-on-hover) so it stops visually promising in-page filtering; still navigates to all 3 destinations, no real filtering added (explicitly out of scope). **#204:** `/backtesting`'s "Popular Simulations" horizontal-scroll row had no affordance at 375px — added a "Swipe →" label next to the section header, `md:hidden` so desktop's unchanged 2-col grid doesn't show it; scroll behavior untouched. **#205:** signup password-strength bar had no text label — added one reading the existing `passwordScore()` value ("Weak"/"Fair"/"Strong"), shown only once the user has typed something; no new strength-calculation logic. **#206 (row-crowding half only):** `/status` subsystem rows used `flex items-center justify-between` with no wrap, so a 2-line-wrapped detail string sat flush against the status badge (confirmed 0px gap). Fixed by restructuring to `flex-col` below `sm` (badge drops to its own line) and the original horizontal layout at `sm:` and up — plain `flex-wrap` alone wouldn't have forced a break since the text can already shrink-wrap without the row overflowing. The ingestion-uptime-sparkline half of #206 is untouched, out of scope. **#199:** Replay Tour's demo media referenced `/onboarding/welcome-demo.gif`, a file that doesn't exist — every open threw a console 404 even though the `onError` fallback ("Demo preview coming soon") already covered it visually. `WELCOME_DEMO_SRC` set to `""`; component now treats an empty src as already-failed and skips mounting an `<img>`/`<video>` at all, so no request (and no 404) fires; swap in a real asset path later to re-enable. **Docs:** `docs/brain/06_COMPONENTS.md` / `docs/claude_project/06_COMPONENTS.md` (CommodityChip + MarketImpactAssessment prop/behavior). No route/table/prompt/standing-rule changes — 05_API, 04_DATABASE, 18_AI_ENGINE, 10_DECISIONS untouched. **Verified:** `tsc --noEmit` clean repo-wide; `pnpm --filter web test` (all `CommodityChip.test.tsx` / `MarketImpactAssessment.test.tsx` cases, including the two price-subtext cases) pass; `eslint` on the 7 touched files shows only pre-existing, unrelated issues at lines this change didn't touch (confirmed via `git diff --stat`). **Not verified this session:** no live-browser walkthrough — task didn't ask for one and none of the 6 fixes are visual/rendering bugs by the token-discipline policy's carve-out, so verification stayed at type-check + unit tests + diff review per `feedback_token_discipline`.
- Vercel Ignored Build Step deploy-skip bug, fixed — 2026-09-24 (commit `acac825`). Root cause: `apps/web/vercel.json`'s `ignoreCommand` diffed `HEAD^ HEAD` only, so any docs-only sync commit immediately following a real code commit (this repo's own standing workflow) made Vercel see "no relevant change" and silently cancel the deploy — the code commit itself never got built. Confirmed this had already happened twice this thread (`38dfe8b`, `e1b0942`): production was still serving `235112e`, pre-dating the whole thread, despite everything since being pushed to `main`. Fixed by switching to `npx turbo-ignore --fallback=HEAD^1`, Vercel's own recommended Turborepo pattern, which diffs against `$VERCEL_GIT_PREVIOUS_SHA` (last successfully deployed commit) instead of the immediate git parent. Changelog: `docs/brain/14_CHANGELOG.md` v0.92.0. **Not verified this session:** whether production has actually redeployed onto a post-`235112e` commit since — no Vercel deployment-log check run.
- #186 Phase 6 — 768px tablet pass, closed — 2026-09-24 (commit `e1b0942`). Swept 22 of 23 verifiable routes at 768px with the dual-check method (document-level `scrollWidth` + nested-overflow walk); onboarding stays excluded per standing policy. Found one shared root-cause bug on 6 pages: `backtesting`, `settings`, `watchlist` (`WatchlistClient.tsx`), and `watchlist/[symbol]` each built their own redundant `md:fixed md:inset-0 ... md:right-[260px]` wrapper — a full re-implementation of the sidebar-offset positioning the shared `(dashboard)/layout.tsx` already provides via `md:ml-[256px]` on a normal-flow div, PLUS an unjustified `right-[260px]` reservation with **no matching right-panel content anywhere in any of the 4 files** (checked via grep for any `w-[260px]`-ish counterpart — none exists). `alerts` and `calendar` had the identical mistake via `md:mr-[260px]` margin instead of the fixed-position technique; `alerts` additionally stacked its own `md:ml-[256px]` on top of the shared layout's already-applied one, doubling the left offset to 512px. At 1440px desktop the wasted space (up to ~520px combined) was invisible — plenty of room either way — but at 768px it left real content as little as ~190px to work with, well below what anything inside needed. This is why `calendar`'s desktop table appeared to need 777px of internal horizontal scroll (previously assumed to be normal table behavior, per `08_CURRENT_STATUS.md`'s "0px, clean" note — that check evidently didn't catch the nested nature of this) — the table itself was fine, its container was pathologically narrow. **Fix:** `right-[260px]` → `right-0` (4 files, fixed-position wrapper); `md:mr-[260px]` removed entirely + `md:ml-[256px]` duplicate removed on `alerts` (2 files, margin technique) — 6 files total, one-class-or-less change each. Two secondary fixes surfaced by the same investigation: `watchlist`'s floating "+" action button was anchored to the old dead-space boundary (`md:right-[292px]`, floating in the middle of a gap that no longer exists) — moved to `md:right-10`, aligned with the container's own `md:p-10` right padding. `alerts`' "Built from" source-citation link (`max-w-[220px] md:max-w-[420px]`) still overflowed even with the wrapper fixed, because 420px doesn't fit in the ~430px now genuinely available once you subtract the numbered-list-item indentation ahead of it — added an `md:max-w-[280px] lg:max-w-[420px]` tablet tier so it only claims the larger width once there's realistically room (1024px+). One near-miss caught before it shipped: an early pass at the `alerts` fix added `md:mt-0` to also strip the top margin at desktop, copying the pattern from the other 4 files — wrong for `alerts`, since those 4 switch to `md:fixed` positioning (needing `md:top-16` instead), while `alerts` stays normal-flow and the fixed `TopBar` (`fixed top-0 ... z-40`, confirmed via source — fixed at every breakpoint, not just mobile) would have been left with nothing clearing it at 768px+. Caught by checking `TopBar.tsx`'s actual positioning before trusting the copy-paste, corrected before verification. Settings' 5th "DATA" tab (42px past the visible tab strip at 768px) is not a bug — the tab row already has `overflow-x-auto` built in, confirmed reachable by scroll, not lost. **Verified:** `tsc --noEmit` clean; all 6 fixed pages re-checked at 375px (mobile, no regression), 768px (0px document overflow, confirmed via script across every route), and 1440px (desktop, screenshot-compared, no visual regression, `TopBar` clearance confirmed correct on both `alerts` and `calendar` after the near-miss above). Full per-page findings in the commit message; this entry is the terse pointer per this file's "keep deep research out of the repo" rule.
- #186 button/link parity audit + 3 fixes — 2026-09-24 (commit `38dfe8b`). Founder asked for a strict scope pivot: stop comparing mobile against Stitch mocks, instead verify every interactive control that exists on **desktop** also exists and works on **mobile** — no new functionality, parity only. Method: grepped `apps/web/app` + `components` for every Tailwind responsive-hide pattern (`hidden md:`/`lg:`/`sm:`) — confirmed the only mechanism this codebase uses to remove elements below a breakpoint (no JS-level `useMediaQuery`/`innerWidth` gating exists anywhere) — 16 instances found, each traced to source. 4 real gaps found and fixed in this commit:
  1. **Replay Tour had no mobile trigger.** `TopBar.tsx`'s "Help & Guidance" icon (opens `HelpModal`'s replay-tour action) is `hidden md:block`; the tour itself auto-plays fine on mobile for first-run users (device-agnostic check in `dashboard/page.tsx`), but a mobile user who dismissed/completed it had no way to see it again. Fix: added a "Replay Tour" entry (`md:hidden`, same `startTour()` + navigate-to-`/dashboard` logic as `HelpModal`'s `handleReplayTour`) to `Sidebar.tsx`'s mobile drawer footer, between Help and Logout.
  2. **Map's "About the Global Tension Index" tooltip was desktop-only.** Desktop's tension panel (`map/page.tsx`) has an ⓘ button explaining the score; `MobileTensionSheet.tsx` rendered the same score + Kinetic/Cyber/Diplomatic breakdown with no explainer. Fix: added the same tap-to-toggle info button + tooltip (identical copy) above the breakdown bars in `MobileTensionSheet.tsx`.
  3. **`MediaImpactTag` + source-confirmation badge missing from dashboard stream rows.** Both `hidden md:*` in `dashboard/page.tsx`'s compact stream-row list (neither fully lost — `MediaImpactTag` already showed in the mobile featured/hero card; source-confirmation showed on the full event-detail page — but absent from the scannable list). Fix: un-hid both; added `flex-wrap` to the row's metadata line so `MediaImpactTag` drops to its own line on narrow screens instead of overflowing (dot/timestamp/FreshTag stay `shrink-0` on the first line); source-confirmation badge left in its existing flex position since the headline's `flex-1 min-w-0` already absorbs it without overflow.
  4. **Quick View modal (desktop-only, replaced by full navigation on mobile) — flagged, not fixed.** Explicitly a design judgment call (is full-page nav an acceptable substitute for an in-place peek?), not a clear bug — left for the founder rather than guessed at.
  Also corrected a mistake from the prior session's Stitch-mock comparison: the map's Kinetic/Cyber/Diplomatic tension breakdown was reported as "needs building" — it was already shipped in `MobileTensionSheet.tsx`, just behind a tap-to-expand sheet that wasn't opened during that pass. **Verified:** `tsc --noEmit` clean; all 3 fixes live-browser-tested by interaction (not just DOM presence) — Replay Tour actually opens the tour modal, the tension tooltip renders with correct text/position (224×97px, not the desktop instance's zero-size hidden node — two elements share the aria-label, had to disambiguate by `offsetParent`), `MediaImpactTag` confirmed wrapping cleanly at 375px with no clipping. Desktop equivalence confirmed by computed style at 1440px: Sidebar's new button correctly `offsetParent === null` (hidden) on desktop, `MediaImpactTag` unaffected in the desktop stream row (screenshot-compared, identical to pre-fix). One real snag mid-session: the long-running dev server on :3100 (up since before this session) was serving stale SSR HTML for `Sidebar.tsx` even after a hard navigate — confirmed via a direct authenticated `fetch()` diff, not assumed; a single restart (per `CLAUDE.md`'s "restart it once, move on") resolved it, `RATE_LIMIT_SAFE_MODE=true` already set in `.env.local` so zero Upstash quota spent. Full audit + a separate detail-sheet doc for the 6 not-yet-built "new functionality" ideas raised this session (recommended builds #1-3, calendar sync, map recenter/layer-toggle, status+accuracy merge) were delivered as Claude Artifacts, not repo docs, per this file's "keep deep research/rationale out of the repo" rule — this entry is the terse pointer.
- Day Mode dead-control removal, Settings > Appearance — 2026-09-24 (commit `f2d0d6d`). Founder reported clicking "Day Mode" did nothing. Root cause: `useTheme()` (`next-themes`, `app/(dashboard)/settings/page.tsx`) was called with **no `<ThemeProvider>` mounted anywhere in the app** (`app/providers.tsx`, `app/layout.tsx`) — without a provider, `next-themes` falls back to a stub context (`setTheme` a no-op, `theme` always `undefined`), so `setTheme("light")` was a literal no-op and the card's own `theme === "light"` selected-state/checkmark check was permanently false too. `app/layout.tsx` also hardcodes `<html className="... dark" data-theme="dark">` and an inline `style={{ backgroundColor: "#0e0e0e" }}` on `<body>`, independent of React state — would have fought a working switch anyway. **Checked whether "just add a ThemeProvider" would fix it — it wouldn't:** `globals.css` (lines 77-116) has a complete `[data-theme="light"]` CSS-variable block, but `tailwind.config.ts`'s later "STITCH GENERATED EXACT TOKENS" color block redefines the exact same class names the app's real markup uses (`bg-surface-container` in 26 files, `text-on-surface` in 28, `background`/`primary`/etc., plus 21 files with literal `text-white`) as hardcoded hex strings, fully disconnected from those CSS variables — the light-mode vars are dead code, nothing reads them. Making Day Mode actually re-theme the app would mean re-pointing Tailwind color classes across roughly 50 files app-wide, not a settings-page fix. **Founder decision (asked via `AskUserQuestion`, not assumed):** remove the dead control rather than build light mode out — see ADR 027 (`docs/brain/10_DECISIONS.md`) / D31 (`docs/claude_project/10_DECISIONS.md`). Fix: `useTheme` import + hook call removed from `page.tsx`; "Day Mode" card deleted; the remaining "Trader (Default)" card is now static (`grid-cols-1`, single card, always-selected border/checkmark, no `onClick`, no `theme` dependency at all). Backlog C4 ("Populate settings Appearance tab") struck and marked rejected in `docs/claude_project/09_BACKLOG.md`, not left as an open queue item. **Verified:** `tsc --noEmit` clean across the whole `apps/web` project (0 errors — worktree's `apps/web/node_modules` was actually missing entirely at session start, `pnpm install --frozen-lockfile` fixed it, unrelated to the fix itself). Live-browser-checked per `CLAUDE.md`'s UI-interaction-bug exception to the Playwright-restriction guidance (this bug report was itself "clicking X does nothing"): signed into the standing test account, navigated Settings → Appearance, confirmed only the single "Trader (Default)" card renders with its checkmark always showing, no dead second option, no console errors.
- Remaining "Sentinel"/"Autonomous" AI-tool branding removed (D29/ADR 025 enforcement, #186-adjacent) — 2026-09-24 (commit `3526000`). A grep swept the repo for the standing "never call it an AI tool"/D29 exclusion and found 3 real remaining instances (a 4th, `alerts/page.tsx:705`'s "Configure Sentinel Alert Threshold" modal title, was handled separately by a parallel session — see below): **(1)** Dashboard right-sidebar widget (`app/(dashboard)/dashboard/page.tsx`, "Market & Intelligence Synthesis" panel) was labeled "SENTINEL" with copy "Autonomous monitoring active across configured intelligence nodes." plus a hardcoded, always-true "System status: Operational" line — the latter was fabricated (never derived from real data; the page already has a real, honest, data-driven status source in `IngestionStatusBanner` at the top of the same page). Renamed to "SYSTEM STATUS", rewrote copy to "Signal collection and classification across configured regions and sources.", and removed the fabricated "Operational" line entirely rather than wiring up a second live-status fetch (would have duplicated `IngestionStatusBanner`'s existing job — the widget still links to `/status` for real detail). **(2)** `events/[id]/page.tsx`'s alert-creation modal title — same "Configure Sentinel Alert Threshold" string duplicated from the alerts page, renamed to "Configure Alert Threshold" to match. **(3)** `components/layout/TopBar.tsx` — the no-session fallback user object (lines ~51-55) and two JSX defaults (~229, ~281) shown before real Supabase user data loads used `"Terminal Sentinel"` / `"sentinel@bluebeacon.com"`; replaced with a neutral `"Account"` name and blank email fallback. Left the static `"v2.4.0-STABLE"` version tag next to it alone (not AI-tool framing, just a cosmetic version string) — docs corrected accordingly (see below). **Deliberately not touched:** `apps/web/proxy.ts:160` — that "sentinel" is a code comment referring to a sentinel *value* (CS pattern), unrelated to branding. Docs: corrected `docs/claude_project/01_PRODUCT.md`, `07_DESIGN_SYSTEM.md`, `06_COMPONENTS.md` — all three previously described "Terminal Sentinel v2.4.0-STABLE" as current, intentional cosmetic branding (stale as of this fix; the version tag stays, the Sentinel name doesn't). No route/table/prompt/new-decision — this enforces the existing D29/ADR 025 exclusion, not a new standing rule. **Verification:** grep confirms zero remaining "Sentinel"/"SENTINEL" matches in the 3 files touched; `git diff` reviewed line-by-line (plain string/JSX-text swaps only, no logic changes). Could not run `tsc --noEmit` — this worktree has no installed `node_modules` (`apps/web/node_modules` absent, pre-existing environment gap unrelated to this change) — and did not live-browser-verify (copy/branding-only fix, not a rendering/interaction bug, so per the token-discipline rule this didn't warrant spinning up a dev server + browser).

**Reconciliation note (2026-09-24):** this Sentinel cleanup was accidentally split into two parallel background sessions that both grepped the whole repo and both found/fixed `events/[id]/page.tsx`'s modal title and `TopBar.tsx`'s fallback name — the first session's task ("fix the alerts modal") was told to also grep for other instances "just in case," which is exactly what the second, separately-scoped session ("fix the dashboard/event/TopBar instances") was independently doing at the same time. Reconciled by keeping this entry's `events/[id]` fix and its `TopBar.tsx` fallback (`"Account"` / blank email) since it landed first and is the more complete of the two (also covers the dashboard widget, which the other session's scope never included); took only the other session's unique contribution — `alerts/page.tsx`'s modal title — since that file wasn't touched here. The other session's own `TopBar.tsx` fallback choice (`"Terminal User"` / `"guest@bluebeacon.com"`) was discarded, not applied. Lesson for next time: when splitting a branding/copy grep-and-fix across parallel sessions, scope each one to an explicit file list and skip the "grep for more, just in case" instruction on all but one of them.
- #188 classifier-extraction country fix — 2026-09-24 (commit `50ff7cf`). Follow-up to the Phase 1/2 investigation (see `claude/188_...md` §3): GDELT's displayed signal country/map pin was really `sourcecountry` (the publishing outlet's country, e.g. "US" for a US outlet covering a Middle East story), not the event's location. Fix: `classifyEvent()` (`claude.service.ts`) now also returns a `country` field — the specific country Claude judges the event to have physically happened in, read from the article text — sanitized by a new `sanitizeCountry()` (free text, not an enum; rejects empty/"null"/"unknown"/"unclear"/"n/a"/"none", caps 100 chars). `geo-resolver.ts`'s `resolveGeoCoords()` gained a 4th param and now prioritizes title chokepoint keywords → classifier country → raw source-country fallback → region → global jitter (was: title → source-country → region → global). Updated `gdelt-collector.ts` (the actual bug), `rss-collector.ts` + `gnews-collector.ts` (both already always passed `country: null` here — now use the classifier's real read instead of hardcoding "Global"), and `reconciliation.ts` (recovers orphans from any source, same GDELT-sourcecountry bug applies there). **Deliberately left `acled-collector.ts` unchanged** — ACLED's own `country`/`latitude`/`longitude` are real per-incident data (not an outlet's country), so routing it through the classifier's guess would be a regression, not a fix; see diagnosis in the commit. No DB migration — same `signals.country`/`lat`/`lng` columns, different value winning. Docs: corrected `docs/claude_project/16_DATA_PIPELINE.md` §2.1 and `docs/brain/15_INGESTION_PIPELINE.md` §2.3 — both previously implied GDELT hands this collector `ActionGeo_CountryCode`/`ActionGeo_Lat`/`ActionGeo_Long`/CSV Event Export fields; confirmed this session `gdelt-collector.ts` calls the DOC 2.0 "artlist" JSON API instead (`url, title, seendate, socialimage, domain, language, sourcecountry` only). Also updated `docs/claude_project/18_AI_ENGINE.md` §2 (new `country` field in the classification prompt/schema).
- Alerts page "BUILT FROM [object Object]" bug fixed — 2026-09-24 (commit `66e7f55`). `apps/web/app/api/alerts/recent/route.ts` built each match's `sourceLabel` as `raw_data.source ?? raw_data.domain`, but `raw_data.source` isn't a consistent shape across collectors: NewsAPI/GNews write it as an object (`{id, name, url, country}`), other collectors write a plain string. When it's an object, the truthy check picked it over the string fallback and the alerts page (`app/(dashboard)/alerts/page.tsx:664`) coerced it straight into a template literal, rendering the literal text "[object Object]" instead of a source name. Confirmed on the standing test account (romantannison@gmail.com), rule "News — Middle East — Severity 6+", match "Trump threatens to 'drive Iran to hell' - but looks dangerously weak" (`raw_events.id = bf39a13e-5f9b-43c0-bc21-a8e40b0f6193`, `raw_data->'source' = {"id":"3fd81fec...","url":"https://inews.co.uk","name":"The i Paper","country":"gb"}`). Fix: extract `.name` when `source` is an object, use it directly when it's a string, only then fall back to `domain`. DB check: 426 of 519 non-null `raw_events.raw_data->'source'` rows are object-shaped (all 426 have `.name`), so this wasn't a one-off — it affected any match whose source came from an object-shaped collector. Verified via direct SQL replicating the new resolution logic (`source_label` → "The i Paper"), not browser-walked (no dev server running in this worktree; data-correctness fix, not a rendering/UI bug, per session-efficiency rules).
- #186 real overflow bugs found + fixed after founder reported still seeing horizontal-scroll issues — 2026-09-23 (this commit). **Important process finding: every #186 phase's "0px overflow, verified" claim (this one included, until now) was checking only `document.documentElement.scrollWidth` vs `clientWidth`. That check has a blind spot:** any ancestor with `overflow-y: auto` (used as the main scrollable content wrapper on `dashboard`/`settings`/`backtesting`/`watchlist`/`watchlist/[symbol]`/`events/[id]`/`map`) causes the browser to auto-compute `overflow-x: auto` on that SAME element too (a real, if obscure, CSS Overflow spec rule: if one axis is non-`visible`, the UA forces the other to `auto` rather than leaving it ambiguous). That creates a second, independent, invisible-scrollbar-on-mobile scroll region *inside* the page that never shows up in `document.documentElement`'s own numbers — content can overflow it by 100+ px while the page itself reports a clean 0. Founder caught this by eye (real screenshots, not just relying on prior "0px" claims) — correctly did not trust the claim per `/CLAUDE.md`'s standing rule to verify status claims rather than take them at face value. **Two real bugs found this way, both fixed, `apps/web` only, `className`-only:** (1) **`/dashboard`'s Intelligence Feed rows** — the row (`flex min-w-0 flex-1 items-center gap-6`, `app/(dashboard)/dashboard/page.tsx`) packs a severity dot, a `w-20` timestamp, `FreshTag`, `MediaImpactTag` (only when `media_impact_entity` is set), the headline (`flex-1`, **no `min-w-0`**), a conditional source-confirmation badge, and a trailing `chevron_right` icon, all `shrink-0` except the headline — at 375px the fixed-width siblings alone (worst case, with `MediaImpactTag`'s "[MEDIA-IMPACT]" badge present) exceeded the row's available width by up to 150px, and because the headline had no `min-w-0` it couldn't shrink to compensate, so the excess pushed the trailing chevron up to 136px off-screen — reachable only by an undiscoverable horizontal swipe inside that one row. Fixed: `min-w-0 truncate` on the headline (so it degrades gracefully instead of demanding its full text width), `MediaImpactTag` and the confirmation badge now `hidden md:block`/`hidden md:inline-flex` (both are supplementary — the full context is still shown on the event's own detail page, nothing is deleted, per [[feedback_functionality_parity_and_stitch_scrutiny]]), `gap-6`→`gap-2 md:gap-6` and timestamp `w-20`→`w-16 md:w-20` to reclaim a bit more room for the headline. Also fixed in the same investigation, found via the same nested-overflow audit: `FilterBar.tsx`'s non-stacked ("bar") field layout (used only by `/dashboard`) put each field's label beside its control (`flex items-center gap-2`) rather than above it — fine on desktop, but on a 375px screen the Commodity/Region/Severity/Time-range fields (Time-range alone needs ~277px for its 4 buttons) don't fit next to their own labels, and because "bar" mode doesn't stack, `flex-wrap` on the outer row can't rescue it (wrapping moves whole fields to new lines, it doesn't shrink a field's own internal label+control layout) — same root defect, contributed to the same 150px figure. Fixed: `flex flex-col gap-1 md:flex-row md:items-center md:gap-2` (stacks label above control below `md`, identical to the existing "stack" layout mode already used safely by `/map`'s sidebar — desktop unchanged). **Pre-existing, unrelated to any #186 tap-target work — last touched Phase 1 (copy-only) for `dashboard/page.tsx` and never for `FilterBar.tsx`'s field layout:** confirmed via `git log` before fixing, so this bug predates this session's tap-target commits; it was simply never caught because no prior phase's verification method could see it. (2) **`/settings`'s section tabs** — `TABS` has **5** entries (`ACCOUNT`, `NOTIFICATIONS`, `APPEARANCE`, `SECURITY`, `DATA`) but the earlier tap-target pass (this session, PHASE 61) only ever measured the first 4 visible-in-viewport buttons at 360px and missed that `DATA` was already off-screen — a pre-existing bug that predates this session too (`flex gap-8`, no wrap, no scroll, `gap-8`=32px is desktop-only appropriate spacing), the min-h-44 fix didn't cause it or make it worse (it only added height, not width). Fixed: `overflow-x-auto` + `shrink-0` per tab + `gap-4 md:gap-8`, mirroring the same intentional, already-precedented horizontal-scroll-region pattern used by `/accuracy`'s and `/calendar`'s data tables — desktop (`md:gap-8`, all 5 fit with no scroll needed) unchanged. **Full 24-page re-sweep run with a corrected method** (not just `document.documentElement` — walks every element, flags any with `overflow-x: auto`/`scroll` whose `scrollWidth > clientWidth`, at 360/375/414px): only two other hits, both confirmed intentional/pre-existing-safe on inspection — `/accuracy`'s and `/calendar`'s own data tables (`overflow-x-auto` wrapping a real `<table>`, the standard, correct mobile-table pattern, not a bug). Every other page: zero hits at all three widths. **Verified, per [[feedback_functionality_parity_and_stitch_scrutiny]]'s two-check rule:** live interaction on top of the size checks — dashboard's My Feed/Desk buttons and Commodity `<select>` still fire their handlers and update state correctly at 375px; settings' tab-click still switches the visible section (confirmed via actual rendered content, not just the active-tab class); both pages re-checked at 1440px, confirmed pixel-identical desktop behavior to before (dashboard row: full `MediaImpactTag`/confirmation badge/full-width headline restored; settings: all 5 tabs inline, no scroll). `tsc --noEmit` clean. **Process fix, so this class of bug isn't missed again:** the "manual pre-merge checklist" convention in `09_BACKLOG.md` is updated to include the nested-overflow walk, not just the `document.documentElement` check.

**⚠️ CORRECTION, 2026-09-24 (see the Phase 7 entry below and ADR 026): the `/dashboard` fix described above was necessary but not sufficient.** It genuinely closed the hidden-overflow bug, but the result — `min-w-0 truncate` letting the headline shrink to whatever's left over — is visually near-useless (headlines truncate to ~8-10 characters). "0px overflow, verified" was wrongly read as "done." Do not treat the `/dashboard` row as closed; see Phase 7 for the real diagnosis and an attempted-and-reverted follow-up fix.
- #186 Phase 5b (tap-target, per-page dense controls) — 2026-09-23 (this commit). `apps/web` only, 16 files, all `className`/CSS-only (`git diff --stat` audited: every file is a 1-4 line diff, no handler/prop/logic line touched). Closes out the deferred list from the shared-components pass. **New standing rule applied this pass (founder, 2026-09-23):** every fix verified two ways — a `getBoundingClientRect()` size check AND an actual interaction (click a button and check its resulting state changed, not just its size) — per [[feedback_functionality_parity_and_stitch_scrutiny]]. **Fixed, one shared constant clearing ~10+ elements across 5 files:** `lib/utils.ts`'s `SELECT_CLASSES` (used by every filter `<select>` on dashboard/calendar/backtesting/watchlist/settings) — added `min-h-[44px] md:min-h-0`; live-verified a `<select>`'s real `onChange` still fires and updates state correctly at 360px, and reverts to its original ~34-39px height at 1440px. **Fixed, per-page:** dashboard (My Feed toggle, Oil/Grain/Metals Desk buttons — verified `aria-pressed` flips on click), `FilterBar.tsx`'s time-range buttons (shared by dashboard + map sidebar), `LoadMoreButton.tsx` (shared by dashboard + map), alerts (Feed/Watchlist/Lab nav buttons, "+ New Alert Rule"), watchlist (commodity toggle chips, remove/"close" buttons — restructured via `p-[13px] md:p-1` hit-slop padding since they're `position:absolute`-anchored by one corner so padding-growth doesn't require repositioning, "Force Refresh"), calendar (UTC/Local toggle), `/watchlist/[symbol]` ("Back to Watchlist", 1M/6M/1Y/3Y/5Y range buttons — verified `setChartRange` fires), `/backtesting` (4hr/24hr/48hr/7d horizon buttons — verified `setHorizon` fires and active-state styling updates), `/settings` (ACCOUNT/NOTIFICATIONS/APPEARANCE/SECURITY tabs, `items-end` alignment to preserve the underline-touches-text look — verified tab content actually switches, not just visual state), `/help`'s `FeedbackForm.tsx` ("SEND FEEDBACK", `h-10`→ effectively 44px via `min-h`), `/admin/metrics` + `/admin/service-status` ("← Back to dashboard" links) and `ServiceStatusClient.tsx` (group/leaf tabs, "Load data", RSS feed select). `/map`: MapLibre's own `NavigationControl` zoom in/out buttons fixed via a **CSS override** (`globals.css`, mobile-scoped `@media (max-width: 767px)`, matching the file's existing precedent for scoping MapLibre control overrides to `.map-page-root`) rather than touching library internals — desktop stays untouched since these buttons are already desktop-hidden by design (mobile-only addition from Phase 4, no scroll-wheel on phones); `MobileTensionSheet.tsx`'s "tune" filter-open button (`w-10 h-10` → `w-11 h-11`, unconditional since this component is mobile-only). **Deliberately left alone, with reasoning:** the "Filter the feed" 18×18 element flagged in the original scan is actually Driver.js's onboarding-hint beacon (`components/onboarding/FeatureHints.tsx`, third-party `driver.js/hints`) — a transient, self-dismissing (localStorage-gated, seen-once) pointer, not a recurring app control; resizing it risks fighting the library's own positioning/animation for near-zero real benefit, so left as-is. MapLibre's attribution link (`/map`, 51×14) also left alone — required third-party attribution. **Environment issue hit and resolved mid-verification, not a code bug:** after editing all 16 files, live verification initially showed several pages (backtesting, calendar, settings — but not dashboard) still serving pre-edit `className` values, with one page (`/backtesting`) throwing a React hydration-mismatch error on every load. Root-caused via `read_network_requests`: the dev server's Turbopack build was returning `304 Not Modified` for those pages' JS chunks despite the source files being changed on disk — a stale/missed file-watcher event for a subset of the 16 edited files (same class of issue as the orphaned-process file-watcher bug earlier this session, but this time on a **freshly started** server, so it's a Turbopack flakiness issue, not a stale-process issue). Fixed by killing the dev server, clearing `.next/cache/{webpack,turbopack}`, and starting fresh — confirmed via `read_network_requests` that all previously-304'd chunks recompiled, hydration error gone on repeat navigation (console error count stayed flat across reloads, confirming no new errors), and every page's `className`/size now matches the source. Re-signed into the standing test account after the restart (session was lost). **Verified:** `tsc --noEmit` clean (run twice — after the initial batch of edits and again after the server restart). All touched controls checked at 360px (correct 44px sizing, click/change handlers fire and produce the expected state change) and 1440px (revert to original desktop size, 0px overflow, no visual regression).
- #186 Phase 5b (tap-target, shared components) — 2026-09-23 (this commit). `apps/web` only. **Full 24-page discovery sweep run first** (JS `getBoundingClientRect()` snippet from the handoff, corrected to exclude off-canvas elements — the first pass without that correction produced false positives from the closed mobile sidebar drawer, which sits at `x:-232` when closed but still reports nonzero width/height since it's `translate`d off-canvas, not `display:none`). Found tap-target violations on ~20 of 24 pages; founder chose to scope this pass to the shared components that clear the most pages per fix, deferring ~13 page-specific dense controls (see below) to a follow-up. **Fixed:** (1) `TopBar.tsx`'s notification bell and avatar buttons — both were icon-sized only (24×30, 32×32) on every authenticated page. Restructured (not just className: moved `relative` positioning to a new inner wrapper span so the unread-count badge dot stays anchored to the icon, not the enlarged button) so the button's hit area is `min-w-[44px] min-h-[44px] md:min-w-0 md:min-h-0` while the *visible* icon/avatar stays its original size — verified live: 44×44 hit area at 360px, unread badge still correctly positioned on the bell icon (not floating), reverts to exactly 24×24 / 32×32 at 1440px (no desktop regression), 0px overflow. (2) `PublicHeader.tsx`'s logo link ("Blue Beacon Research" text, 17×20) — `min-h-[44px] md:min-h-0` added; verified 17×44 at 360px, 156×20 (unchanged) at 1440px, on both `/status` and `/accuracy`. (3) Auth pages — `login/page.tsx` ("Forgot password?" link + password eye-toggle button), `signup/page.tsx` (eye-toggle), `reset-password/page.tsx` (eye-toggle), `verify/VerifyClient.tsx` ("Resend email" button, `h-10`→`h-11`), `forgot-password/page.tsx` ("Send reset link" button, `h-10`→`h-11`) — all verified 44px live at 360px, reverting correctly at 1440px. **Deliberately left alone:** the "Sign up" / "Sign in" / "sign in" / "Sign up again" links at the bottom of each auth page — these are inline text within a sentence ("Don't have an account? Sign up"), which WCAG 2.5.5 Target Size explicitly exempts from the 44×44 minimum; forcing padding onto an inline link mid-sentence risks visual overlap with adjacent lines for no real accessibility gain. **tsc --noEmit clean.** **Confirmed a real dev-environment gotcha, not a product bug, while verifying:** the stray orphaned `next dev` process killed for the footer fix (previous entry) was restarted cleanly and worked correctly for the rest of this session — no repeat of the stale-file-watcher issue. **Still open (explicitly deferred, not forgotten):** per-page dense controls found in the same sweep but not fixed this pass — dashboard (category/region/page `<select>`s at 39px, Oil/Grain/Metals Desk buttons at 32px, "Load more" at 36px, filter icon at 18×18), alerts (Feed/Watchlist/Lab toggle at 32px, "+ New Alert Rule" at 34px), watchlist ("ADD COMMODITY" select at 39px, commodity chips at 28px, "close" ×-buttons at 38px, "Force Refresh" text link at 18px), calendar (filter selects at 39px, UTC/Local toggle at 32px), `/watchlist/[symbol]` ("Back to Watchlist" at 24px, 1M/6M/1Y/3Y/5Y range buttons at 28px), `/map` (zoom in/out at 29×29, filter "tune" button at 40×40), `/backtesting` (region/commodity selects at 39px, 4hr/24hr/48hr/7d window buttons at 30px), `/settings` ("Help & FAQ" link at 17px, ACCOUNT/NOTIFICATIONS/APPEARANCE/SECURITY tabs at 34px), `/help` ("SEND FEEDBACK" at 40px), `/admin/metrics` + `/admin/service-status` ("← Back to dashboard" link at 18px; service-status also has category/source-filter buttons and "Load data" at 30px). MapLibre's own attribution link (51×14 on `/map`) was left alone on purpose — required third-party attribution, not app code. All 24 routes reconfirmed 0px horizontal overflow during this sweep — no new overflow regressions found.
- #186 Phase 5b (tap-target, homepage footer) — 2026-09-23 (this commit). `apps/web/app/page.tsx` only, `className`-only (`git diff` audited before commit). Handoff's `HANDOFF_186_PHASE5.md` said the footer's shared className (`text-[12px] md:text-[11px] font-bold text-on-surface/60 hover:text-primary transition-colors uppercase`) occurred 9 times; re-counted via `grep -n` at session start and found **10** — the `/terms` link carries the same string as a prefix of a longer className (`... uppercase flex items-center gap-1"`), which a `grep -c` for the exact standalone string (ending in a bare closing quote) undercounts. Fixed all 10: the 9 simple links got `min-h-[44px] inline-flex items-center` appended via a scoped `replace_all` (matched on the string immediately followed by `"`, which excludes `/terms`); `/terms` got `min-h-[44px]` added to its existing `flex items-center gap-1` by hand, no duplicate `items-center`. **Verified:** live in-browser at 360px — all 10 links measured `height: 44` via `getBoundingClientRect()` (were 15px before), 0px horizontal overflow; re-checked at 1440px — still 0px overflow, all still 44px tall (min-height applies at every breakpoint, same as the pattern already shipped on `TopBar`/`MobileTabBar`/`WatchlistClient`/`PublicHeader`), screenshotted, no visual regression. `tsc --noEmit` clean. **Session note:** verification initially blocked by a stale orphaned `next dev -p 3100` process left running from the prior session — its file watcher had stopped picking up edits (confirmed two ways: live `getBoundingClientRect()` still showed 15px after the edit, and `curl` of its server-rendered HTML had no `min-h-[44px]` at all). Killed the process chain and started a fresh `next dev -p 3100`; fix then verified correctly both ways. Not a code bug — an environment artifact, noted here in case it recurs.
- #186 Phase 5b (partial) — `/events/[id]` tab-clipping bug (real functionality loss) + 3-column meta row — 2026-09-23 (this commit). `apps/web` only, `className`-only. **Real bug, not a Stitch-mock question:** the ANALYSIS/HISTORICAL/MAP/SOURCES tab row (`Tabs`/`TabsList`/`TabsTrigger` from `components/ui/tabs.tsx`) has no responsive prefixes; at 360/390px its intrinsic content width (4 tabs × padding + `gap-8`) exceeds the viewport, and because the row sits inside layout that doesn't propagate scroll, the "sources" tab (and part of "map") was **silently clipped out of reach entirely** — not scrollable, not visible, genuinely unusable — confirmed via `getBoundingClientRect()` before the fix and via clicking the tab after. Fixed with `overflow-x-auto flex-nowrap` on `TabsList` + `shrink-0` per `TabsTrigger`, `gap-4 md:gap-8` (desktop `gap-8` unchanged). Verified: scrolled the tab strip to its own max scrollLeft, confirmed "sources" fully on-screen, clicked it, confirmed the content panel actually switched (real interaction, not just visual). Second, smaller fix in the same investigation: the PUBLISHED/VERIFICATION/LOCATION meta row (`grid-cols-3`, no prefix) crammed 3 columns into ~100px each at 360px, wrapping "1 High-Integrity Sources" across 3 lines — changed to `grid-cols-1 sm:grid-cols-3` (screenshotted before/after, dramatic readability improvement) with the middle column's decorative `border-x` made `sm:border-x` so no orphan border shows in the single-column mobile layout. **Verified:** `tsc --noEmit` clean, full `apps/web` test suite passing, `next build` clean. Desktop 1440px re-verified after the fix: `tabsRowScrollable:false` (fits naturally, no scroll triggered), 0px overflow, 3-column meta row visually unchanged, sidebar's `ALPHA` badge confirmed still absent (Phase 5a). `/admin/metrics` also checked this session — already fully responsive (live founder usage stats, 2-column stat grid, table `overflow-x-auto` from Phase 1) — zero changes needed. `ProductTour.tsx`/`FeatureHints.tsx`/`NotificationConnectPrompt.tsx`/`NotificationConnectModal.tsx` grepped for fixed-pixel-width patterns — zero hits in all four, consistent with the rest of this phase's finding that most overlay/modal components are fluid by construction. **Still open at end of session (see handoff):** the 44×44 tap-target sweep was in progress — homepage footer links confirmed still ~15px tall (9 occurrences of one shared className in `app/page.tsx`), fix identified (`min-h-[44px] inline-flex items-center`, same pattern already used on `TopBar`/`MobileTabBar`/`WatchlistClient` this phase) but **not yet applied** when the session paused for a context handoff. Phase 5b otherwise not started: remaining unresponsive files beyond what's been checked, plus Phase 6 (768px tablet pass).
- #186 Phase 5a — 4 more pages with the exact Phase-2 bug class, worse than the original; ALPHA badge + one military-clearance string removed — 2026-09-23 (this commit). `apps/web` only, `className`-only where layout-related. **Found by grepping the whole codebase for the exact bug pattern** (`\b(w|ml|mr|left|right)-\[[0-9]+px\]` outside an `md:` guard) rather than re-auditing file-by-file — surfaced `settings/page.tsx`, `backtesting/page.tsx`, `watchlist/WatchlistClient.tsx`, and `watchlist/[symbol]/page.tsx` all sharing the byte-identical unconditional wrapper `fixed inset-0 left-[256px] right-[260px] top-16 ... p-10` that caused Phase 2's `/alerts`/`/calendar` bug. **This instance is worse than that one was**: screenshotted before the fix — at 360px the content wasn't just scrolled/clipped, it rendered in a narrow sliver with most words truncated (`"Co" "Wa"` instead of "Commodity Watchlist", `"ENERG"` instead of "ENERGY") and a large dead black zone on the left half of the screen, because `left:256px` + `right:260px` on a 360px `fixed` box computes a near-zero/negative available width. Fixed identically to Phase 2: `md:fixed md:inset-0 md:left-[256px] md:right-[260px] md:top-16` + `mt-16 md:mt-0` + `p-4 md:p-10`, so `>=768px` renders the byte-identical unconditional values these pages always had. **Two more bugs in the same investigation:** `WatchlistClient.tsx`'s page-header row (`flex items-end justify-between`, no wrap) had the identical title-vs-control overflow shape as the original `/alerts` bug — fixed with `flex-wrap gap-4`, same pattern. Its floating "add" button used a desktop-tuned `right-[292px]` and a `bottom-8` that didn't account for the new `MobileTabBar`'s 60px height — screenshotted as a barely-visible sliver poking above the tab bar before the fix; now `right-4 bottom-[76px] md:right-[292px] md:bottom-8`. **Checked and confirmed NOT bugs, left untouched:** `backtesting/page.tsx`'s 4 fixed-width filter fields (`min-w-[240px]`/`w-[180px]`×2/`min-w-[200px]`) sit inside an already-`flex-wrap` row — each width is well under any tested viewport, wraps cleanly, no fix needed; `dashboard/page.tsx`'s `w-[260px] ... hidden xl:flex` sidebar and `events/[id]/page.tsx`'s `w-full xl:w-[320px]` aside are already correctly mobile-first (base = full-width/hidden, override at `xl:`); `NotificationPanel.tsx`'s `w-[360px]` measured 0px overflow at exactly the 360px floor (border-box sizing keeps the 1px border inside the declared width). **Also this batch (D29 military-copy removal):** the `ALPHA` status badge next to the sidebar brand text removed (founder decision, does not reflect current product status) — confirmed via full-codebase grep this was the only such badge (a second grep hit, `AccessLimitedModal.tsx`'s "Node: BB-ALPHA-09"-style string, is the *kept* atmosphere category, untouched); `AccessLimitedModal.tsx`'s footer `SEC_LVL: ALPHA` (a security-classification-style label matching the military-clearance pattern D29 says to drop) replaced with `STATUS: LIMITED`, which actually describes what the modal is (a waitlist gate) rather than inventing a fake clearance level. **Also audited and confirmed already mobile-safe, zero changes needed:** all 6 auth pages (`login`, `signup`, `forgot-password`, `reset-password`, `verify`, `confirm` — all use fluid `width:100%; max-width:440px` card patterns, genuinely responsive despite carrying zero Tailwind breakpoint prefixes, a reminder that the "zero prefix" audit metric is a proxy, not the actual test), `HelpModal.tsx`, `card.tsx`/`tabs.tsx` (both fully fluid/flex-relative by construction). **Confirmed dead code, not fixed:** `components/ui/dropdown-menu.tsx`, `components/ui/select.tsx`, `components/ui/badge.tsx`, `components/ui/separator.tsx`, `components/signals/SignalCard.tsx`, `components/layout/PriceTicker.tsx` — zero importers each, verified via grep before skipping. **Verified:** `tsc --noEmit` clean, full `apps/web` test suite passing, `next build` clean. Live at 360px with real signed-in data: all 4 fixed pages screenshotted before/after (settings shows real profile `test`/`romantannison@gmail.com`; backtesting shows 5 real named simulations; watchlist shows real category chips + live Copper/Brent/Corn prices; `watchlist/COPPER` shows a real 1M price chart, 6.82, -0.23%) — zero overflow at 360/390/414/1024/1440 on all four. Desktop 1440 screenshotted and confirmed pixel-equivalent to pre-fix (sidebar, header row, chip layout, FAB position all unchanged). One unrelated finding: a "1 Issue" pill visible in mobile screenshots is confirmed **not in the DOM** (`querySelector` found nothing) — an injected browser-tool overlay, not app code, not a real bug.
- #186 Phase 4 — map bottom sheet, fixes the 0%-map-visible bug + a real pre-existing popup bug — 2026-09-23 (this commit). `apps/web` only. **Before implementing, did a full functional inventory of `/map` (all ~1020 lines) and a founder-requested critical review of the Stitch mock against it** rather than reskinning blind: the mock omits the 24h tension trend sparkline entirely (kept visible per founder decision, not dropped), invents a fabricated `LATENCY ~5M` stat (not included — same class of thing already stripped twice this session), and shows dashboard-style feed-card badges/labels (`COMMODITY` badge, `ANALYZE` link) that don't match this page's actual stream item shape — kept the real card format (URGENT/SIGNAL badge, VIEW DETAILS, click-to-pan-and-popup) instead of copying the mismatched mock markup. Founder also approved adding MapLibre `NavigationControl` (+/- zoom, mobile-only) as new functionality beyond strict parity, since phones have no scroll-wheel; declined a locate/geolocate control (no feature reads location anywhere in this app). **Implementation:** new `components/map/MobileTensionSheet.tsx` (`md:hidden`, tap-to-expand peek/expanded, owns no data — every value is a prop passed from `map/page.tsx`'s existing state) plus a `md:hidden` filter modal reusing the identical `<FilterBar>` desktop uses (verified safe to double-mount: `FilterBar` has zero internal `useState`/`useEffect`/`window` usage, pure controlled component). Desktop's two panels (`filtersCollapsed`/`streamCollapsed` sections, previously unconditional at *every* viewport — the actual root cause of the map being 0% visible on a phone) gained `hidden md:block`/`hidden md:flex`. Two new purely-presentational booleans on `MapPage` (`mobileSheetExpanded`, `mobileFiltersOpen`) — no new business logic, only which UI renders. **Real bug found and fixed along the way, independent of the mobile-sheet work:** `MapSignalPopup.tsx`'s width formula (`min(22rem, calc(100vw-26rem))`) evaluates **negative** on any viewport under ~416px, and its `left` used the desktop-only `offsetLeft` prop unconditionally — meaning tapping any map marker on a phone already did nothing useful, before this phase touched anything else. Fixed via a `--popup-offset` CSS custom property referenced only inside `md:`-scoped Tailwind values, so `offsetLeft`'s value/meaning is completely untouched and desktop positioning math is provably identical (see verification). **Verified, mobile (390px, live signed-in session, real data):** map render went from 0% visible to 100% visible (screenshotted before/after); zoom buttons functional and correctly positioned above the sheet, not overlapping it; peek header shows live score/sample-size/sparkline; expanded state shows real 35/55/10 breakdown percentages and real stream items (titles, fresh tags, VIEW DETAILS) — same live data as desktop; a cluster tap correctly triggered `getClusterExpansionZoom` (zoom changed 1.8→confirmed via `map.getZoom()`) — proving the untouched click-handler logic still fires correctly under the new layout; an individual-point tap opened the popup with real signal data (severity badge, commodity chips, summary) at `left:16px width:358px position:fixed`, i.e. exactly centered with even margins, vs. the previous negative-width/off-screen result. **Verified, desktop (1440px):** `filtersVisible`/`streamVisible` both `true`, mobile tab bar `display:none`, zero horizontal overflow, zoom buttons correctly absent (hidden via the `.map-page-root`-scoped CSS rule); popup click landed at `left:656px` = exactly `24rem+1rem` (400px) relative to the map root's own left edge (256px, from the sidebar) — the identical desktop formula, `position:absolute`, width `22rem` (352px) — proving byte-for-byte unchanged desktop math, not just "looks the same." **Testing-tool note:** the `computer` (mouse) input tool could not reliably click canvas-rendered MapLibre markers by screenshot-derived pixel coordinates (three attempts missed) — resolved by finding the live `Map` instance through the React fiber tree (`__reactFiber$*` key → walk `.return` → scan hook `memoizedState` for a ref exposing `queryRenderedFeatures`) and dispatching real `MouseEvent`s at MapLibre's own `map.project()`-computed coordinates on `map.getCanvasContainer()`. This is a verification-technique detail, not a product change. `tsc --noEmit` clean, full `apps/web` test suite passing, `next build` clean (concurrent with the still-running `next dev`, confirmed no corruption). Phase 5 (remaining ~20 unresponsive files, `ALPHA` badge removal, tap targets) and Phase 6 (768px tablet pass) not started.
- #186 Phase 3 — mobile bottom tab bar — 2026-09-23 (this commit). `apps/web` only. New `components/layout/MobileTabBar.tsx`: FEED/MAP/ALERTS/WATCHLIST/MORE, `md:hidden`, mounted from `(dashboard)/layout.tsx` alongside the existing `Sidebar`/`TopBar`. MORE opens the same off-canvas drawer `Sidebar` already renders (`useUIStore.mobileSidebarOpen`) — no second nav surface, Calendar/Backtesting/Settings/Help/Logout stay exactly where they were. Icon names and active-route logic copied verbatim from `Sidebar.tsx`'s `NAV` array so desktop and mobile never drift. `<main>` gained `pb-[60px] md:pb-0` so content clears the fixed bar. `TopBar.tsx`'s mobile hamburger button deleted — it opened the identical drawer MORE now opens, redundant. **Verified:** `tsc --noEmit` clean, full `apps/web` test suite passing, `next build` clean (concurrent with the still-running `next dev` on the same `.next` dir — confirmed no corruption: the live authenticated dev-server session and page content were unaffected afterward). Live browser at 390px: tab bar renders with correct icons/labels, FEED highlighted green (`#4edea3`) on `/dashboard`, unread badge shows the real `unreadCount`; navigated to `/map` and `/watchlist` — MAP and WATCHLIST tabs each independently computed the active green color, 0px overflow on both. MORE button's `onClick` verified via direct dispatch (`btn.click()`) after the `computer` input tool hit repeated 30s timeouts on this specific click — confirmed via `read_console_messages` and a live `curl`/cookie check that the page and dev server were both healthy throughout, so the timeout was an input-layer tool flake, not a real hang: drawer opened (`translate-x-0` + backdrop present) and closed (backdrop click) correctly. Desktop 1440: bar confirmed `display:none`, 0px overflow, layout pixel-unchanged. **Workflow note:** this phase used the long-running `next dev` server switched to during Phase 2 (see that entry for why) — zero server restarts needed, one sign-in covered the whole phase's verification.
- #186 Phase 2 — `/alerts` 335px + `/calendar` overflow root-caused and fixed — 2026-09-23 (this commit). `apps/web` only, all `className`-only edits, zero logic/handler/data changes (diff-audited before commit). **The original diagnosis (TopBar search box) was wrong** — `min-w-0` fixes on `TopBar.tsx`'s search wrapper were applied (harmless, kept) but measured **zero effect** on the actual overflow. Root cause, found by scanning the live DOM for the element with the furthest-right edge rather than guessing further: `app/(dashboard)/alerts/page.tsx:345` and `app/(dashboard)/calendar/page.tsx:348` both had a hardcoded `ml-[256px] mr-[260px]` on the page's own outermost wrapper — a byte-identical copy-pasted anti-pattern **duplicating** the sidebar-width margin the shared `(dashboard)/layout.tsx` already applies (`md:ml-[256px]`), unconditionally on every viewport including mobile, where there's no sidebar to clear at all. Fixed by gating both values behind `md:` (`md:ml-[256px] md:mr-[260px]`) and `p-8`→`p-4 md:p-8`, so desktop (`>=768px`) renders the exact unchanged value it always has. A second, smaller bug in the same investigation: `alerts/page.tsx`'s header `<section className="flex justify-between items-end mb-8">` had no wrap handling between the H1 and the Feed/Watchlist/Lab tab-switcher pill, causing a non-monotonic 33px residual specifically at 390px (present at 390, absent at 360 and 414, because the H1's word-wrap geometry only collided with the pill in that narrow band) — fixed with `flex-wrap` + `gap-4` + `min-w-0` on the title container. Also fixed alongside: the `truncate max-w-[420px]` source-attribution span (`alerts/page.tsx:663`) flagged by an independent audit of the same investigation, made responsive (`max-w-[220px] md:max-w-[420px]`). **Verified — full 360/390/414/768/1024/1440 matrix, both routes, on both a production build (`next build`+`next start`) and (after switching workflow) a `next dev` server with a live authenticated session:** `/alerts` is now 0px overflow at every width except 768px, where **183px persists by design** — at exactly the `md` breakpoint the real (unduplicated) sidebar margin correctly reactivates, colliding with the already-known, not-yet-addressed Phase 6 tablet-squeeze problem (sidebar 256px + this page's now-correctly-single 260px right margin leaves little room at exactly 768px); this is pre-existing behavior, unchanged by this fix, not a regression. `/calendar` is 0px overflow at **all six widths including 768**. `/dashboard` and `/watchlist` re-checked at 360 and 1440 — 0px both, confirming the `TopBar.tsx` edits introduce no regression on shared chrome. `tsc --noEmit` clean, full `apps/web` test suite passing, `next build` clean. **Workflow correction, worth recording:** every earlier verification attempt in this phase used a killed-and-restarted `next start` production server per edit; the founder's browser session was being signed out by every restart (traced to the Supabase client treating the brief connection drop as an auth failure), not by natural token expiry — repeated manual re-logins were a self-inflicted cost, not a real system limitation. Switched to a single long-running `next dev` (Fast Refresh) server for the remainder of verification; zero further restarts needed, session persisted across the full final check. **Also caught and immediately fixed, unrelated to the fix itself:** while adding `RATE_LIMIT_SAFE_MODE=true` to `apps/web/.env.local` via a shell append, the file's missing trailing newline caused it to land on the same line as `ADMIN_EMAILS`, corrupting that value (`...romantannison@gmail.comRATE_LIMIT_SAFE_MODE=true`) for one command cycle before the dev server was next started — caught by re-reading the file immediately after, fixed before any server restart could pick up the bad value, confirmed `.env.local` is gitignored (`git check-ignore` — no leak risk, no bad state ever reached a running process). **Not this commit:** Phase 6 (768px tablet pass) — the 768px number here is pre-existing, tracked, and deliberately out of scope.
- #186 Phase 1 (copy integrity, mocks-analysis follow-up) — 2026-09-23 (this commit). `apps/web` only. **(1a)** Backtesting footer (`app/(dashboard)/backtesting/page.tsx`) dropped three fabricated claims that survived the `4651f6c` cleanup of this same file: `SYSTEM HASH: 88F9-AX21-KL88`, `LATENCY: 12ms`, `DATA INTEGRITY: 100% Verified` (nothing computed the last two). Footer is now just the brand line, centered. **(1b)** Dashboard feed row badge (`app/(dashboard)/dashboard/page.tsx`) — the `{n}% CONFIDENCE` pill was real classifier data but inconsistent with #178/#179, which deliberately replaced raw confidence percentages with source-confirmation labels elsewhere. Replaced with `sourceConfirmationLabel(item.sourceConfirmation)` (same helper `MapSignalPopup` already uses), null-hidden per that same precedent. **Deliberately left unchanged:** the separate "Confidence" stat tile in the featured/hero card (`data-tour="confidence-score"`, ~line 462) — this is a different UI element anchored by `ProductTour.tsx`, not the badge the founder approved changing; touching it would be undiscussed scope expansion. **(1c, docs-only)** Both `07_DESIGN_SYSTEM.md` files marked superseded — neither matches shipped reality (claude_project describes an unshipped `#10B981` palette; brain is a stale Stitch snapshot). Canonical design system is now `docs/stitch_mobile/tactical_intelligence_terminal/DESIGN.md`, verified token-identical to live `tailwind.config.ts`/`globals.css` (one typo corrected: its `primary-container #4EDE93` should be the live `#4EDEA3`). New **D29/ADR 025** in both `10_DECISIONS.md`: the Stitch mobile mocks (`d10626b`) are layout references only — 10 of 11 checked strings in them are fabricated content already deliberately removed from live code days earlier (`GENESIS-X_V4`, the 71% accuracy stat, `Sub-second synthesis…`, `40-Year Intel Archive`, ACLED-as-active, etc.), plus new fabrications never shipped (price targets, `BULLISH SPIKE` calls, invented latency/SLA/packet-loss/failover-DC metrics, untracked commodities, `SENTINEL AI SYNTHESIS` branding, a `.mil`/`SECURITY CLEARANCE` military register, and `GEOSIGNAL PRO` — not even this product's name). Decision: take geometry from Stitch, copy/data from live code only. Also locked: drop the `ALPHA` badge; keep terminal-atmosphere copy, drop military-roleplay copy; mobile tab bar is FEED/MAP/ALERTS/WATCHLIST/MORE (not the mock's ordering — ALERTS promoted for its live unread badge). **Verified:** `tsc --noEmit` clean; `next build` succeeds; `npm test` in `apps/web` all suites pass (including the existing `market-impact-assessment.test.ts` assertion `sourceConfirmationLabel("reported") === "Reported claim"`); production build (`next start -p 3100`) checked at 360px and 1440px on `/backtesting` and `/dashboard` — old strings/badge confirmed gone, zero horizontal overflow at either width, featured-card confidence tile confirmed still present at 1440 (untouched). **Honest gap:** did not visually confirm the new source-confirmation badge painting on a specific labeled row in the browser — of the currently paginated feed items only 2 of 2946 signals carry a non-null `sourceConfirmation` and neither surfaced within a reasonable page/load-more depth. Confidence instead comes from: the live API confirming real `sourceConfirmation: "reported"` data exists, the unit test confirming the label function's output, and the identical call pattern already shipped and working in `MapSignalPopup.tsx` per #179. Phases 2–6 (TopBar overflow, mobile tab bar, map bottom sheet, remaining screens, tablet pass) not started.
- "LIVE"/"real-time" copy honesty sweep — 2026-09-23 (this commit). `apps/web` only, copy/text-only, no logic change. Grepped the whole `apps/web` tree case-insensitively for standalone "LIVE" and "real-time"/"real time"; collectors actually check for new articles roughly every 30 minutes (not instantly), so any label implying instant delivery was replaced with "updates roughly every 30 minutes" framing (or the misleading word dropped where a full cadence phrase didn't fit the string). **Homepage** (`app/page.tsx`): hero badge "Live — monitoring active global conflicts" → "Monitoring active global conflicts — updates roughly every 30 minutes"; "View live signals" → "View recent signals"; "Live research feed" heading → "Recent research feed" (the paragraph directly below it already correctly said "not in real time" — the heading now agrees with the paragraph next to it instead of contradicting it). **Dashboard** (`app/(dashboard)/dashboard/page.tsx`): subtitle "Real-time global signal monitoring" → "Global signal monitoring, updates roughly every 30 minutes"; Recent Signal Stream badge "LIVE DATA FEED ON" → "UPDATES ROUGHLY EVERY 30 MIN" (this exact badge was already flagged in `lib/help-faq.ts`'s own "What does LIVE mean?" answer as "a static label — it does not check whether collectors last succeeded", i.e. already known to be misleading); sidebar "REAL-TIME SYNTHESIS" → "SYNTHESIS — UPDATES ~30 MIN". **Map page** (`app/(dashboard)/map/page.tsx`): the right-hand "Live Intelligence" panel (pulsating dot, sits directly above the real `IngestionStatusBanner` health line) → "Intelligence Stream"; aria-labels "Live intelligence stream"/"Collapse live intelligence panel"/"Expand live intelligence panel" → drop "live"; "No live stream data" → "No stream data"; `endLabel="End of live stream"` → "End of stream". **Help/onboarding**: `HelpModal.tsx` "view real-time incident details" → "view incident details"; `ProductTour.tsx` first dashboard tour step "your live signal feed" → "your signal feed". **Error copy**: `lib/user-error-copy.ts` `FEED_DEGRADED_COPY` "The live signal feed couldn't refresh" → "The signal feed couldn't refresh". **Status page** (`lib/status-checks.ts`): Intelligence Feed check detail "REST API & WebSocket live data feed" → "REST API, updates roughly every 30 minutes" — also dropped "WebSocket" from the claim; grepped `apps/backend` and found no WebSocket route registered anywhere (only an internal `ws` transport polyfill for the Supabase client), so the public `/status` page was claiming a delivery channel that doesn't exist as a product feature, not just overclaiming speed. **Consistency fixes alongside the badge changes** (same file, so the FAQ/comment wouldn't quote a label that no longer exists): `lib/help-faq.ts`'s "What does LIVE mean?" answer now quotes the new "UPDATES ROUGHLY EVERY 30 MIN" badge text instead of the retired "LIVE DATA FEED ON"; `components/layout/Sidebar.tsx`'s landmark-uniqueness comment now quotes "Intelligence stream" instead of the retired aria-label text. **Deliberately left unchanged** (different meaning or genuinely out of scope, not silently missed): `app/page.tsx` "View the live track record" (the Accuracy page track record is continuously/currently updated — "live" there means ongoing, not instant, and isn't about ingestion speed); pricing tier feature "Live signal feed" on the Analyst plan vs. free-tier "Delayed Feed (4h)" (a tier-differentiation/business-copy claim — grepped for an actual 4h-delay implementation and found none, so whether the paid tier really is undelayed relative to the ~30min baseline is an open, unverified question, not something this pass should silently rewrite); `app/status/page.tsx`'s `<meta description>` "Real-time infrastructure operational status" (the `/status` page itself runs its checks fresh on every request — `dynamic = "force-dynamic"` — so that claim is about the status page's own freshness, not signal cadence, and is accurate); `IngestionStatusBanner.tsx`'s "Live ingestion" label (this is the one label in the product that actually checks real collector recency — accurate, left as the canonical honest status line); the honest `HELP_FAQ_ITEMS` "Is the Economic Calendar live market data? No." answer (already correctly says it is *not* live). **apps/backend is untouched and now out of sync**: `apps/backend/src/lib/search-catalog.ts`'s `SEARCH_FAQ_ENTRIES` "What does LIVE mean?" entry still quotes the retired "LIVE DATA FEED ON" wording — the task that requested this sweep was explicitly scoped to `apps/web`; that backend copy needs the same one-line fix in a follow-up. **Also found, not fixed, flagging per the doc-precedence rule rather than silently picking one:** `docs/brain/14_CHANGELOG.md` is missing the `v0.84.0`/`v0.85.0` entries that `docs/claude_project/14_CHANGELOG.md` PHASE 51/52 already point at by name (confirmed via `git log -1 --format=%cd` — the claude_project copy was touched almost an hour after the brain copy) — this entry uses `v0.86.0` to avoid colliding with those already-referenced-but-missing numbers; backfilling v0.84.0/v0.85.0 itself is a separate docs-sync task, not part of this ship. **Verified:** re-grepped `apps/web` case-insensitively for `\bLIVE\b`/`real-time`/`real time` after all edits — every remaining hit is either a code comment, an already-honest string, or one of the deliberately-left-unchanged items above. `tsc --noEmit` clean. `npm test` in `apps/web` — all suites pass, including `help-faq.test.ts` (still 10 FAQ items, still has the required substrings). Not browser-walked — copy/text-only change, no rendering-behavior risk, per the CLAUDE.md session-efficiency rule.
- Next 16 `middleware.ts` → `proxy.ts` rename — 2026-09-23 (this commit). `apps/web` only, founder decision (option B: rename deliberately rather than leave the deprecation or defer to Next 17). Next 16.2.0 deprecated the `middleware` file convention in favour of `proxy`; running `next build` **auto-applies the codemod**, so the rename kept reappearing in `git status` after every build and risked being committed by accident inside an unrelated change. `git mv apps/web/middleware.ts apps/web/proxy.ts` + `export async function middleware` → `export async function proxy`. **Nothing else changed** — the Supabase SSR session logic, the `isProjectReady` gate, and the `matcher: ["/((?!_next/static|_next/image|favicon.ico).*)"]` config are byte-identical. Deliberately NOT touched: the six comments in `app/(auth)/*` and `lib/supabase.ts` that use the word "middleware" conceptually (they describe the SSR-cookie rule correctly, and churning them would bloat an auth-critical diff), and `zustand/middleware` imports in `store/*` (an unrelated package path). **Verified — both directions of the gate, on the production build (`next build` + `next start -p 3100`), against a live founder-signed-in session:** block path `curl` with no cookie — `/dashboard` and `/alerts` both **307 → /login**, byte-identical to the pre-rename baseline captured on the same server; public `/`, `/login`, `/accuracy` all 200 (proxy runs without throwing). Admit path with a real session — `/dashboard` (2 `sb-` cookies present, renders), `/watchlist`, `/alerts` all admitted, no redirect. `tsc --noEmit` clean; build output registers `ƒ Proxy (Middleware)` and the deprecation warning is **gone**. **Diagnosis:** mid-verification the browser session vanished (zero cookies on `localhost`), which looked like the rename had broken session handling. It had not — the Supabase access token had simply expired (~1h TTL) and the SSR client correctly cleared the stale cookies. Confirmed by re-signing in and re-running the admit path, rather than assuming. This matters because this exact file caused the 2026-08-28 site-wide 504 outage (`getUser()` on every route), so the rename was held uncommitted until both gate directions were actually observed.
- #186 Phase 1+2 — responsive foundations (mobile/tablet rework) — 2026-09-23 (this commit). `apps/web` only. **(1A) iOS input zoom:** single `@media (max-width:767px)` rule in `globals.css` sets `input:not([type=checkbox]):not([type=radio]), select, textarea { font-size:16px !important }`. `!important` is required and deliberate — `login/page.tsx` and `signup/page.tsx` apply `fontSize:"14px"` through inline `inputStyle` objects, which outrank normal stylesheet rules. One rule covers all 15 files with form fields. **(1B) Sub-12px type floor:** all 328 hardcoded arbitrary sub-12px Tailwind sizes rewritten `text-[Npx]` → `text-[12px] md:text-[Npx]` across 36 files (210× 10px, 58× 11px, 51× 9px, 9× 8px) — phone floor 12px, desktop size byte-identical. Verified 0 bare sub-12px remain, 328 `md:`-prefixed now exist, 0 double-applications. **(1C) Table clipping:** `accuracy/page.tsx:219` and `admin/metrics/page.tsx:119` had `overflow-hidden` wrappers that silently clipped table columns on narrow screens → `overflow-x-auto`; accuracy's 4-column table also got `min-w-[520px]` so it genuinely scrolls instead of squishing. `backtesting`, `calendar`, `service-status` already had `overflow-x-auto` — untouched. **(2) Public header overflow:** the duplicated `h-16 … px-8` header in `accuracy/page.tsx` and `status/page.tsx` extracted to new shared `components/layout/PublicHeader.tsx` (`badge` prop = "TRACK RECORD" / "SYSTEM MONITOR") with `px-4 md:px-8`, `min-w-0` + `truncate` on the wordmark, `shrink-0` on logo and Terminal link, badge `hidden md:inline`, and `min-h-[44px] md:min-h-0` on the Terminal tap target. Now-orphaned `Link` / `Logo` imports deleted from both pages. **Verified:** production build (`next build`) succeeded; `tsc --noEmit` clean. Measured at 360px in a real browser on the built output (`next start -p 3100`) — `/accuracy` **111px → 0px**, `/status` **117px → 0px**, accuracy table parent now scrolls, badge `display:none` on phone, `/accuracy` sub-12px rendered-element count **0**. Input rule confirmed present in built CSS chunk and in CSSOM; a freshly-injected `<input type=email>` computes to **16px**. **Diagnosis:** the long-running dev server on :3010 was serving a stale CSS chunk (identical hash+bytes after `touch`), which made the input fix look like it had failed — the rule was correct all along; verification was moved to the production build. Port :3000 is a *different* local project ("RichFeed"), not BBR. **Independent re-audit corrections to the #186 research doc:** `/status` overflows 117px (the doc never measured it and only inferred it); `/accuracy` is 111px at 360px, not 96px; the homepage overflows **0px** at 360px, so that finding does not reproduce; files carrying any breakpoint prefix is **19**, not 22; per-file sub-12px counts in the doc were ~20% high (alerts is 40, not 51); `admin/metrics` has the same `overflow-hidden` clipping bug the doc said it merely lacked a wrapper for. **Not this commit:** Phases 3–6 (see priority queue) — the 21 zero-responsive files, the map, the 768px tablet pass, tap targets, and the regression test.
- ACLED claims removed from web copy — 2026-09-23 (`9772bcc`). `apps/web` only, three strings, no logic change. ACLED is confirmed inactive in production (no credentials configured). **(1)** `app/page.tsx` homepage "01. Event Detection" card: "Public sources such as ACLED and GNews are scanned..." → "...such as GNews are scanned..." (GNews is real/active, kept). **(2)** `app/(dashboard)/dashboard/page.tsx` `coverageLine`: dropped trailing "+ ACLED". **(3)** `lib/status-checks.ts` `checkDataPipeline()` detail string: dropped "ACLED, ". Left untouched: `apps/backend`'s real ACLED integration (`acled.service.ts`, `acled-collector.ts` — working code, just missing credentials) and the founder-only `/admin/service-status` page (`ServiceStatusClient.tsx`), which already shows ACLED honestly as never-run. Also fixed a now-stale description of the old coverage-line string in `docs/brain/08_CURRENT_STATUS.md`'s #126 entry. **Verified:** grep confirms zero remaining ACLED references in `apps/web` outside `ServiceStatusClient.tsx`; `git status` confirms only the 3 named files changed.
- Backtesting fabricated-engine + accuracy-stat removal — 2026-09-23 (`4651f6c`). `apps/web/app/(dashboard)/backtesting/page.tsx` + `apps/web/app/api/backtesting/route.ts`. Removed the always-visible "GENESIS-X_V4" engine name and "Processing 15 years of geo-political volatility markers" line — no real engine or 15-year dataset behind either, and this text was independent of the `isDemo` disclaimer. Replaced with "Scenario Simulator" / "Hypothetical Event Impact" framing that doesn't claim a real engine or year count. Removed the fixed `accuracyPct` (71%, never varies — implied a track record even next to a disclaimer) end-to-end: API `Result` type + `mockResult()`, UI `BacktestResult` type + the "ACCURACY RATE" stat tile, stats grid 5→4 cols. `Math.sin`-based demo point math untouched — a real backtesting engine is backlog #171, needs its own research pass first. **Verified:** `tsc --noEmit` clean on both files; grep confirms zero remaining `accuracyPct` references under `apps/web`. Not browser-walked (copy/text-only change, no rendering-behavior risk per CLAUDE.md session-efficiency rule).
- #178 + #179 list-endpoint materiality fields + map popup source-confirmation label — 2026-09-21 (this commit). `apps/web` only. **#178:** `GET /api/signals` (map / search / dashboard feed) now maps `novelty` / `sourceConfirmation` / `materialityReasoning` with the same `parseNovelty` / `parseSourceConfirmation` / trim-or-null pattern as `/api/signals/:id` (#154). SELECT was already `*`. **#179:** `MapSignalPopup` drops `{n}% confidence`; shows `sourceConfirmationLabel` (Official statement / Reported claim / Speculative / unconfirmed) only when non-null. **Verified:** local `localhost:3010` against live `evavcgfmemwryggdkjmx` — `GET /api/signals?window=all&limit=5` returned novelty 0.45 / `reported` / reasoning on `05f3d25a-…`; pre-gate `1535f3f0-…` (Ellison) returned null for all 3; map popup Ukraine drones = "Reported claim", heuristic Cuba = no badge / no percent. Production map verify after this deploy (local is not the live claim).
- Homepage "N signals tracked" count query (HEAD → body) — 2026-09-21 (this commit). `apps/web/app/page.tsx` `getHomepageStats()` only. Replaced `select("*", { count: "exact", head: true })` with `select("id", { count: "exact" })` so the count travels in the response body, not a Content-Range header (Vercel/fetch can drop that header). Failures now `console.error` the full Postgrest error object. **Verified:** production `https://bluebeaconresearch.com` logged-out HTML vs live `select count(*) from signals` on `evavcgfmemwryggdkjmx` (see commit report).
- Header search = Cmd+K + last-resort fallback — 2026-09-20 (this commit). `apps/web` + backend `SEARCH_PAGE_ENTRIES` keyword sync. **(A)** TopBar search is a button (`aria-label="Open search"`) that sets `useUIStore.commandPaletteOpen`; `CommandPalette` reads that flag (Cmd+K/Ctrl+K still toggles). Removed `searchQuery`/`searchSubmitted` from the store, dashboard `filteredSignals`, and `useSignalFeed`'s `search` param. **(B)** Expanded `STATIC_PAGES` keywords; when the palette settles with zero hits and assist is off or not `ok`, a single static **"Not sure? Try"** item links `/dashboard` (no extra network call). **Verified:** Playwright on standing session `localhost:3010` (not production). All 8 dashboard-chrome pages: header click and Cmd+K open the same palette; header has 0 `<input>`s. Intelligence Feed still lists events and Load more 13→23. Grep: `searchQuery`/`searchSubmitted` gone from `apps/web` source (leftover only in historical `docs/claude_project/13_PROMPTS.md`). Part B: "smb" and "zzqxv" → one **"Not sure? Try" / Intelligence Feed**; "charts" → Pages/Watchlist, no fallback. Network for those queries: existing `GET /api/signals?search=…&sort=relevance` + `POST /api/search/assist` only — no new fallback endpoint. `pnpm --filter web test` + `pnpm --filter backend test` + `tsc --noEmit` clean.
- Docs onboarding catch-up — 2026-09-20 (this commit, docs-only). Last 15 commits already wrote code into this file through the search-quality ship. What a new model would still miss: `21_PROJECT_BRIEFING.md` was August 2026; `00_CURRENT_BBR_CONTEXT.md` was 2026-09-16 with wrong paths; `22_IMPLEMENTATION_LOG` / `CLAUDE_CONTEXT` / brain `13_PROMPTS` read as live; `19_ROADMAP` still unchecked `/accuracy`; architecture files had no search/help/`/docs` section; many headers still pointed at `claude/23_TODO.md` (not in this repo). Those files now point here + `08` + `14` (v0.78.0 / PHASE 45).
- Search-quality fix: Cmd+K fuzzy match + relevance sort — 2026-09-20 (`d195c4e`). `apps/web` + `apps/backend`. **(1)** `CommandPalette.tsx` Pages/Watchlist-commodities/Alert-Rules matching moved from `.includes()` substring to Fuse.js fuzzy+keyword (`fuse.js@7` added to `apps/web`, threshold 0.3, extended-search; logic extracted to `lib/command-palette-search.ts`). `STATIC_PAGES` 7→8 entries: each page gained real user-word `keywords`, plus a missing Economic Calendar (`/calendar`) entry that already existed in the app and in the backend's AI-assist catalog. Threshold tuned 0.4→0.3 after an initial false positive (noise token fuzzy-colliding with "Intel…" inside "Intelligence Feed"). **(2)** New `sort=relevance` (`rank_score = severity / (hours_since + 2)^1.8`, application code, not SQL) on both the Next.js BFF `GET /api/signals` (the route the browser actually hits) and Fastify `GET /v1/signals` (separate API-tier surface, added for parity) — mirrored formula in `apps/web/lib/signal-relevance-rank.ts` / `apps/backend/src/lib/relevance-rank.ts` (not shared-package'd; backend doesn't depend on `@blue-beacon-research/shared`). Command palette's Signals fetch now uses `sort=relevance`; Intelligence Feed page's default sort untouched. **(3)** Found and fixed a stale `docs/claude_project/05_API.md` claim that the BFF signals route proxies to Fastify — it reads Supabase directly and is the only one of the two routes with a `search` param (Fastify's zod schema never had one). Also flagged, not fixed (out of scope): the BFF's existing `sort=severity` is actually recency-first / severity-tiebreak-only despite its name. **Verified:** `pnpm --filter web test` + `pnpm --filter backend test` (3 new suites) + `tsc --noEmit` clean on `apps/web`. Live Playwright on standing session `localhost:3010` (port 3000 was a different local project): "charts"→Watchlist, "what are the commodity news"→Intelligence Feed, "gold"→Gold unchanged, "smb"→still zero results. Live SQL + API against `evavcgfmemwryggdkjmx`: real signal `cfc52d46-…` (severity 6, 2026-09-15) scores ~26x higher than real signal `103ad6c8-…` (severity 9, 2026-08-17) under the relevance formula; live `sort=severity` vs `sort=relevance` on `search=houthi` produced a different top-5 order.
- #174 homepage subtext — 2026-09-20 (this commit). `apps/web/app/page.tsx` only. Landing hero `<p>` is now exactly "Blue Beacon Research — Geopolitical Intelligence for Commodity Traders". Headline ("High-fidelity geopolitical intelligence…") and meta descriptions unchanged. The Sept 18 brand-giant line was already gone from source (homepage copy-integrity rewrite); this replaces the later interim subtext. No "AI" / "AI-powered" in the line. **Verified:** repo grep has none of the old brand-giant phrase; local homepage HTML contains the new sentence and not the old one.
- #175 Tension Index tooltip click-outside — 2026-09-20 (this commit). `apps/web/app/(dashboard)/map/page.tsx` only. `tensionInfoOpen` now closes on `mousedown` outside the "i" button + tooltip (same pattern as `TopBar.tsx` avatar dropdown). Hover preview (`group-hover`) and tooltip copy ("Composite score derived from regional conflict density, kinetic strikes, and maritime disruption metrics.") unchanged — no formula added. **Verified:** Playwright on standing session at `localhost:3010/map` — click "i" pinned tooltip `opacity:1`; click elsewhere `opacity:0`; hover on "i" faded preview back in; unhover `opacity:0`. Tooltip text byte-identical.
- Homepage copy integrity (fabricated claims + sci-fi register) — 2026-09-20 (this commit). `apps/web/app/page.tsx` only. Removed five unverifiable lines confirmed in the source: "System Integrity: 100% Verified", "Latency: 42ms", Pro-tier "40yr Intel Archive" (real #106 feature is 5-year Yahoo history), footer "Encrypted Support" on a plain `mailto:`, and "Sub-second synthesis of geopolitical volatility pulses". No replacement invented numbers. Retoned nav/CTAs/section headers/footer away from defense-contractor register (Tactical Modules, Beacon Stream, Establish Intel Link, Authorize Full Access, Beacon-Alpha Mainframe, Neural Confidence, Sentinel Synthesis, Scenario Lab Access) toward the §4 research language. New `getHomepageStats()` runs a live `signals` `count(*)` on each render (`force-dynamic`) and shows e.g. "N signals tracked". Accuracy/trust proof is a link to the existing public `/accuracy` page — no homepage hit-rate percentage (deliberate; the live 48h number is near a coin flip). Monitor/Analyst/Pro prices unchanged. **Verified:** grep of `page.tsx` has none of the five strings. Local `localhost:3010/` HTML has none either. Live SQL `select count(*) from signals` = **2929**, rendered as "2,929 signals tracked" (same request). Latest preview row `5f55895e-…`. `/accuracy` link present. **Diagnosis:** `signals` RLS is `signals_select_authenticated` only — cookie/anon `createClient()` returns 0 rows to a logged-out visitor; homepage data reads use `getRouteSupabaseClients().supabase` (service role) so the public count matches SQL.
- #143 leftover (partial) — event-detail wiring of `novelty` / `source_confirmation` / `materiality_reasoning` — 2026-09-20 (this commit). `apps/web` + shared Signal types only. No backend/classifier change. `/api/signals/:id` now maps the three already-stored columns onto the response (they were in `select("*")` and unused). MARKET IMPACT ASSESSMENT shows Source confirmation (Official statement / Reported claim / Speculative / unconfirmed) and Novelty (UI buckets: ≥0.7 New development, ≥0.3 Partial update, else Mostly a repeat/reminder) only when those fields are non-null. ANALYSIS tab gets a "Why this signal" `<details>` with the raw `materialityReasoning` string, also null-hidden. SignalQuickView header drops the raw `{n}% confidence` badge (#140 leftover there); MarketImpactAssessment already covers source confirmation when present. Spec items 12 (timeline) and 14 (related-event clustering) not built — no backend mechanism. `relevance` / `materiality_pass` still unread in the UI (`materiality_pass` is a write-time insert gate). **Verified:** live SQL 5 newest novelty-set rows; local `:id` API on `5f55895e-…` returned novelty 0.5 / `reported` / reasoning; Playwright on `localhost:3010/events/5f55895e-…` showed Reported claim + Partial update + Why this signal; pre-gate `a145a3ef-…` showed none of the three and no N/A. `pnpm --filter web test` including new helper + component cases.
- #155 minimal FAQ + feedback form — 2026-09-19 (this commit). `apps/web` + Supabase migration. Static `/help` FAQ (10 questions from live classifier/materiality/accuracy/LIVE copy — not marketing). Feedback form writes `feedback_submissions` (chose table over Resend: web/Vercel has no `RESEND_API_KEY`; that key is on Railway workers for digest). Sidebar Help, Settings, TopBar avatar, Cmd+K Pages, and HelpModal all link to `/help`. `SEARCH_FAQ_ENTRIES` filled from the same answers. **Verified:** migration applied live (`feedback_submissions`); Security Advisor does not list it under `rls_enabled_no_policy`; unauthenticated `POST /api/feedback` 401; standing-test-account `POST /api/feedback` 200 `{ok:true}` landed as row `92fe2bfa-…` (`page_context=/help`); authenticated GET `/help` 200 with all 10 FAQ `<h2>`s + the form. **Not this commit:** live chat / ticketing; Resend copy of submissions; re-embedding the catalog in production (index refreshes on next search-assist call).
- Backend API-docs public-access audit — 2026-09-19 (this commit). Confirmed live `GET https://api.bluebeaconresearch.com/docs` 200 Swagger UI and `/docs/json` 200 OpenAPI 3.0.3 listing `/v1/admin/*`, `/v1/telegram/webhook`, `/v1/search/assist`, etc. with no auth. Swagger UI now registers only when `NODE_ENV` is `development` or `test`; `/docs` is no longer an unauthenticated allowlist path in production. Rate limit already global `@fastify/rate-limit` 60/min (no per-route gaps added). New Fastify route since #138 (`search.routes.ts`) does not return `error.message` / stack to the client. ToS already mentions scraping; reverse-engineering is a founder/legal leftover. JS minification is not a security measure. **Verified:** production curl 200 + grep of swagger/auth/error returns. Post-deploy production `/docs` should 404. **Not this commit:** per-route rate-limit tightening; webhook-test `e.message` leak (pre-#138, authenticated).
- Cmd+K search assist RAG fallback — 2026-09-19 (this commit). Existing `CommandPalette.tsx` Pages/Signals/Watchlist/Alert Rules search unchanged. When it settles with <2 hits, POST `/api/search/assist` → Fastify `/v1/search/assist`: pgvector cosine over `search_content_embeddings`, then one Haiku sentence on the **chat** daily budget (`assertAnthropicBudget` / `isAnthropicBudgetAvailable`). Below threshold or `NO_ANSWER` → `no_confident_answer` (no forced reply). Suggested group is labeled separately. Catalog is real page copy only; `#155` FAQ does not exist in the repo yet (empty `SEARCH_FAQ_ENTRIES`). pgvector was available (`vector` 0.8.0) and not installed — enabled this ship. Embeddings: Anthropic has no API; cheapest Anthropic-partnered model is Voyage `voyage-4-lite` ($0.02/MTok); `VOYAGE_API_KEY` optional, else `local-hash-v1`. **Verified:** mocked unit tests only (backend `search-assist.test.ts` + `answerSearchAssist` mocked client; web `command-palette-assist.test.ts`). No live Anthropic or Voyage call. **Not this commit:** FAQ indexing (#155); setting `VOYAGE_API_KEY` on Railway; a founder-run one query after reviewing the diff.
- Driver.js feature hints (watchlist chips, dashboard filters, RECORD) — 2026-09-19 (this commit). `apps/web` only. `driver.js@1.8.0` Feature Hints mode (not a Joyride/Driver.js tour): one pulsing beacon each on `/watchlist` dropdown+category-chip area, dashboard `FilterBar`, and the event-detail RECORD button. No overlay, no next/back. Seen state is `localStorage` `bbr_hint_seen_<id>` (per-browser, as specified). Clicking the highlighted control also dismisses. RECORD is on `events/[id]/page.tsx` (`handleRecord`), not Backtesting Lab — grep found no Record action there. It writes `{id,title,createdAt}` to `bb.saved_signals` (max 100); nothing in the app reads that key, so the persistent hover tooltip says so. **Verified:** `feature-hints.test.ts` keys/path routing; Playwright on standing session at `localhost:3010` (3000 was another app). Fresh keys: dashboard beacon → popover "Filter the feed" → click FilterBar wrote `bbr_hint_seen_dashboard_filters` and 0 beacons on reload. Watchlist beacon → popover "Add markets from here" → target click wrote `bbr_hint_seen_watchlist_chips`, gone on reload. Event Hormuz row: RECORD hint after scroll-into-view; popover "Bookmark this signal"; RECORD click dismissed, wrote `bb.saved_signals` with that title, hover tooltip matched `RECORD_BUTTON_TOOLTIP`; reload 0 beacons. **Not this commit:** a Recorded Signals list UI; account-synced hint state.
- #146 10 prospect/demo accounts excluded from usage numbers — 2026-09-19 (this commit). `profiles.is_test_account boolean not null default false` (same flag pattern as `onboarding_completed` / `product_tour_completed`). Email confirmation stays ON globally; the 10 accounts were created with Admin `email_confirm: true` only (no signup/login bypass in `apps/web`). Each has onboarding + product tour complete, a (TEST) display name, a plausible watchlist, `plan_tier=pro`. `admin_usage_metrics()` and digest eligibility now exclude the flag. **Verified:** unfiltered `auth.users` 9→19 after seed; founder-console snapshot unchanged (signups 1/0, auth_users 9, waitlist 1, DAU 1, WAU 2). Production login as `demo01@bluebeaconresearch.com` reached `/dashboard` (Maya Okonkwo (TEST), Intelligence Feed) with no confirm-email prompt; that session wrote 1 test `events` row and DAU still 1. Credentials: gitignored `.demo-accounts.local.json` (not in this repo). **Not this commit:** leftover historical `bbr.*` unconfirmed users; standing `romantannison` account is not flagged.
- #142 individual_social_media data update — 2026-09-19 (this commit). Data-only; `classifyEvent()` / `media-impact-watchlist.ts` / UI tag path unchanged. Pre-change live table had **7** rows (4 institutional_official / 2 political_geopolitical / 1 individual_social_media = Elon Musk, `markets=[]`, CNBC-2021 BTC-tweet evidence). **Musk** row updated in place (`unique entity_name`) to the Aug 7 2018 "funding secured" / SEC v. Musk record (~13% TSLA intraday, $20M+$20M 2018 settlement; 2023 NPR/CNN trial coverage). `markets` stays `[]`: TSLA (equity) and BTC are not on the approved allowlist (USOIL/UKOIL/NGAS/XAUUSD/WHEAT/CORN + EURUSD/GBPUSD/USDJPY/USDCHF/USDRUB/USDCNY). **New row:** `Compromised official social-media account` (aliases: AP Twitter hack / hacked AP Twitter / Associated Press Twitter hack) — April 23 2013 AP Twitter hoax, ~$136B S&P 500 drop in 2-3 minutes; framed as compromised/false official account, not a named individual's credibility; `markets=[]` (S&P 500 not tracked). No Trump-named individual_social_media row (positioning decision; office-level `US President` row unchanged). Migration `20260919143000_media_impact_watchlist_individual_social.sql` applied live to `evavcgfmemwryggdkjmx` as `media_impact_watchlist_individual_social`. **Verified:** post-apply SQL = 8 active rows, 2 individual_social_media, Musk/AP `evidence_sources` as written. **Not this commit:** `[Media-Impact]` source-string casing (see report); Trump-named watchlist entry.
- #123 remainder — every event-detail click opens a new tab — 2026-09-19 (this commit). `apps/web` only. `ef73885` left new-tab only on SignalQuickView "View full details". Live feed is inline in `dashboard/page.tsx` (`SignalCard.tsx` is unused on web). Now `target="_blank" rel="noopener noreferrer"` on: dashboard featured/secondary/stream, map popup + sidebar VIEW DETAILS, watchlist Correlated Signals, alerts Recent Matches, event-detail Historical tab, NotificationPanel rows, CommandPalette Signals results. Map sidebar card click still selects the pin (does not navigate); map-not-ready fallback uses `window.open(..., "noopener,noreferrer")`. ProductTour handoff stays same-tab (`router.push`) so Joyride can continue. **Verified:** local click of each listed surface opened `/events/{id}` in a new tab; origin stayed put.
- #145 watchlist empty-state defaults + single range chart — 2026-09-19 (this commit). `apps/web` only. **List:** first paint with no selections shows the shared `COMMODITIES` cards (not an empty dropdown); category chips above the grid add/remove without replacing the existing ADD COMMODITY dropdown. Prefs / a user-cleared list still win after hydrate. Cards use live `/api/prices` + sparkline history — no mock values. **Detail:** `/watchlist/[symbol]` is one chart with 1M / 6M / 1Y / 3Y / 5Y. 1M = 90-day DB series sliced to 30d; 6M+ = existing Yahoo weekly `history-5y` fetch sliced to the window. Correlated-signals list still uses 90 days. Exact 5Y copy preserved: "Yahoo does not have a full 5-year series for this symbol" / "Not enough 5-year price history available for this symbol". **Verified:** standing test account; mocked-null prefs showed 7 default cards + 7 chips (Wheat chip toggled the card off); Copper card later showed live $6.69; USOIL 1M/6M/1Y/3Y/5Y each re-rendered a single `.recharts-surface` (`panelCount=1`); intercepted empty 5Y showed the exact empty sentence; intercepted incomplete 5Y showed "Showing available history from Jan 2024 — Yahoo does not have a full 5-year series for this symbol." Local 5Y API (backend on :3001) USOIL 262 weekly bars, `incomplete=false`.
- Sidebar logo → `/dashboard` + landing hero subtext — 2026-09-18 (this commit). `apps/web` only. Sidebar brand span is now wrapped in `<Link href="/dashboard">` (route confirmed via `app/(dashboard)/dashboard/page.tsx`). Landing hero `<p>` subtext in `app/page.tsx` was set to a short brand line (later replaced by #174); h1 "High-fidelity geopolitical intelligence…" and page/layout `<meta name="description">` left unchanged (those strings are the headline, not the subtext). **Verified:** local click of the sidebar logo from `/map` navigated to `/dashboard`; grep of the Sidebar diff shows only that one span wrapped.
- #141–#144 topic-file catch-up — 2026-09-13 (this commit, docs-only). Filled the files the four ships actually touched but left describing the old world: `04_DATABASE` Table 2 now lists the 8 #141 columns; `15_INGESTION_PIPELINE` + `16_DATA_PIPELINE` now have the materiality reject step; `18_AI_ENGINE` / `13_PROMPTS` B1 / `17_SIGNAL_ENGINE` point at the live classify+gate fields; both `03_ARCHITECTURE` §8; both `10_DECISIONS` (D25/ADR 021, D26/ADR 022, D22 #144 addendum); `05_API` / `06_COMPONENTS` accuracy + BFF leftovers. Multi-horizon work numbered **#144** (`9be929a`). #143 leftover (`relevance` / `novelty` / `source_confirmation` / `materiality_pass` / `materiality_reasoning` unread in the UI) stays open in `09_BACKLOG.md`.
- #144 multi-horizon outcome checkpoints — 2026-09-13 (`9be929a`). `apps/backend` only. `outcome-tracker.ts` now writes `signal_outcomes` for `CHECKPOINT_HOURS_LIST = [1, 4, 24, 48]`; a signal is eligible for a given horizon once it is actually that many hours old (1h/4h/24h are not held until 48h). 48h thresholds, `findClosestPoint()`, and `MAX_PRICE_POINT_DISTANCE_MS` are unchanged. `GET /v1/accuracy` now filters `.eq("checkpoint_hours", 48)` so the new rows cannot enter the #121 headline. **Schema unblocker (live, necessary):** UNIQUE was `(signal_id, asset)`, which cannot store more than one `checkpoint_hours` value per pair. Widened to `(signal_id, asset, checkpoint_hours)` via `20260913190000_signal_outcomes_checkpoint_unique.sql` / MCP `signal_outcomes_checkpoint_unique`. No new column. Existing 48h rows were not rewritten. **Verified against `evavcgfmemwryggdkjmx` before commit** (same `outcome-tracker:once` pattern as #121): worker `signalsProcessed=1690` / `outcomesWritten=9445` / `pairsSkipped=1192` (only the known pre-#87 EURUSD/USDRUB 24h price-gap skips). Counts now `1=3156 / 4=3152 / 24=3126 / 48=3049`. Every 48h-old signal that already had any outcome now has all four horizons (1558/1558). Five oldest 48h sample rows byte-identical to the pre-run snapshot. The original 3038 48h rows still score **2449 / 1152 / 0.470396**. This run also wrote 11 new 48h rows for signals that crossed 48h since the last daily cron (existing 48h behavior) — 48h-only headline is therefore 2457 / 1158 / 0.471306. Pre-deploy live `GET /v1/accuracy` (no filter yet) polluted to 10041 / 2619 / 0.2608; post-deploy must return the 48h-only numbers. **Not this commit:** time-horizon selector on `/accuracy`.
- #143 MARKET IMPACT ASSESSMENT — 2026-09-13 (`9397e3e`). `apps/web` + shared Signal types only. Renames PROJECTED IMPACT → MARKET IMPACT ASSESSMENT on the event detail aside and SignalQuickView. Box now has named parts from #141/#142 columns: `marketMechanism`, affected markets (`commodityImpacts` + `currencyPairImpacts` via confidence-free CommodityChip), direction, `eventCategory` display labels (all 9 enum values), reused `#142` `MediaImpactTag` when `mediaImpactEntity` is set. Empty mechanism + empty impact lists show the exact Caldara & Iacoviello (2022) fallback sentence — no live GPR number. `isPreview` shows a calendar note linking `/calendar`. Next.js `/api/signals` and `/api/signals/:id` map the new fields (`:id` also now returns `currencyPairImpacts`). No raw classifier-confidence percent in this box (#140 left as-is). **Verified:** `pnpm --filter web test` (new helper + component cases) + `tsc --noEmit`; Playwright screenshots of (a) live mechanism+commodity signal `397fc32a-…`, (b) fixture media-impact tag (zero live `media_impact_entity` rows at ship time), (c) live no-direct-match signal `1535f3f0-…` showing the GPR sentence, (d) no visible confidence percent in the box. **Not this commit:** Fastify `/v1` (already `select("*")`), writing new backend columns, #139 remaining severity-math finding.
- #142 live media-impact watchlist — 2026-09-13 (`5473f93`). Replaces #141's hardcoded 7-entry array in `classifyEvent()` with `public.media_impact_watchlist` (applied live to `evavcgfmemwryggdkjmx` via `20260913180000_media_impact_watchlist.sql` / MCP `media_impact_watchlist`). Table is reference data: RLS public-read `to anon, authenticated using (true)`, service-role write only — same policy shape as `signal_outcomes` (#121). Seeded **exactly 7** sourced rows (4 institutional_official / 2 political_geopolitical / 1 individual_social_media). Elon Musk `markets` is `[]` because BTC is not on BBR's approved asset allowlist. Michael Saylor and Cathie Wood were not added. New `signals.media_impact_entity` (nullable text) written by `insertOrMergeSignal` / ACLED / reconciliation from `classifyEvent()`'s new `mediaImpactEntity` field (exact `entity_name` or a known alias; anything else, including unsourced names, sanitizes to null). Backend reads `active=true` through a 10-min in-memory TTL cache (`lib/media-impact-watchlist.ts`, same cache shape as `routes/price-history.ts`). `apps/web`: `[Media-Impact]` tag on the live dashboard cards / stream, SignalCard, SignalQuickView, and event detail (expanded entity + short first-sentence caveat). Copy is a sourced historical pattern, not a forecast or buy/sell. **Verified:** live SQL (7 rows, correct tiers, column + RLS policy present; table not in `rls_enabled_no_policy`); live `getActiveWatchlist()` + heuristic `classifyEvent()` — OPEC story → `mediaImpactEntity: "OPEC"` / pass, diplomatic-talks story → null / fail; unit tests (prompt embeds live list + `mediaImpactEntity`, sanitize drops "Cathie Wood", tag render / no-tag). No live Anthropic spend. **Not this commit:** #143 (other materiality-gate fields still unread in the UI).
- #141 materiality gate — the pipeline's first real "does this mean anything?" reject step. 2026-09-13 (`ea34f28`). `apps/backend` + Supabase migration only. Direct build on #139's audit finding (63% of signals in a 14-day window sat at severity 1-4, many with empty commodity impacts and Claude's own "no market impact" summary, because classification never had a reject step). **Migration** (`20260913160000_signals_materiality_gate.sql`, applied live to `evavcgfmemwryggdkjmx`): 8 new `signals` columns — `relevance`/`novelty` (real, 0-1, range-checked), `event_category` (9-value CHECK enum), `market_mechanism` (text), `is_preview` (boolean, default false), `source_confirmation` (3-value CHECK enum: official/reported/speculative), `materiality_pass` (boolean, NOT NULL default true — existing rows backfill true, only governs future inserts), `materiality_reasoning` (text). **Prompt** (`claude.service.ts`'s `classifyEvent()`): existing fields (severity/confidence/commodityImpacts/currencyPairImpacts/isBreaking/summary/region) untouched; `max_tokens` 500→900. Adds all 8 new fields plus BBR's own materiality-gate instruction (reasonable-investor-inspired: passes only with genuine new information AND one of a real stated mechanism / a hit on a hardcoded 7-entity watchlist [OPEC, Saudi Energy Minister, Russian President, US President, Fed Chair, USDA, Elon Musk, each with a caveat] / genuine armed-conflict relevance — explicitly does NOT gate on whether the claim will turn out true). All new fields sanitized post-parse (unit-float clamp, enum validation, boolean coercion); `materialityPass` fails closed to `false` on anything but an explicit `true`. **Gate enforcement** at all 5 live classify-then-insert call sites (`gnews-collector.ts`, `gdelt-collector.ts`, `rss-collector.ts`, `acled-collector.ts`, `reconciliation.ts` — re-grepped `claude.classifyEvent(` + `.from("signals")` to confirm no 6th site exists): a `materialityPass: false` result now logs the rejection (title/source/reasoning) to console + a new `"rejected"` status in `service_health_events` (`service: "materiality_gate"`, reusing the existing #42 health log) via new `lib/materiality-gate.ts`, then skips the `signals` insert — `raw_events` row stays either way. `ai-classifier.ts` (confirmed dormant) got a comment only, per instruction. **Heuristic fallback** (`heuristicClassify()`) made conservative-consistent: only passes with a validated commodity/currency impact or a watchlist hit; other new fields left null (no real article read). **Novelty hint** (new `lib/novelty-hint.ts`'s `hasSimilarRecentSignal()`): cheap v1 — same country+event_type logged in the last 48h, passed into the prompt as a boolean hint; documented as a coarse proxy, not real duplicate detection. **Verified live against production, real Claude calls** (credits were funded at verification time — worth re-checking against the standing "exhausted" status elsewhere in this doc): ran 3 synthetic test stories through the exact production code path (raw_events insert → novelty hint → real `classifyEvent()` → gate check → conditional signals insert → cleanup) against `evavcgfmemwryggdkjmx`, then deleted every row created. (1) A junk administrative story (Oregon radar-site permitting notice) → `materialityPass: false, isPreview: true, eventCategory: "other_market_relevant", marketMechanism: null`, reasoning naming the specific failed criterion. (2) A pure calendar-reminder story ("Fed meets Wednesday Sept 16," already separately listed in `apps/web/data/economic-calendar.json` as `fomc-2026-09-16`) → `materialityPass: false, isPreview: true, novelty: 0, relevance: 0`. (3) A real Red Sea tanker-strike story → `materialityPass: true, relevance: 0.88, novelty: 0.72, eventCategory: "armed_conflict_security", sourceConfirmation: "reported"`, a genuine non-invented `marketMechanism` string, and a full `signals` row written with every new column populated (id `ebbd3064-…`, deleted after). Heuristic-path gate logic covered by new unit tests in `claude.service.test.ts` (no-impact/no-watchlist fails, validated-impact passes, watchlist-only-match passes) — a forced-fallback production test wasn't possible since real Claude classification was live throughout. **Not this commit:** #142 (replace hardcoded watchlist with a live DB table), #143 (frontend consumption of the new fields — `apps/web` untouched by explicit instruction).
- #140 hide raw classifier confidence on CommodityChip — 2026-09-13 (`fa0f482`). `apps/web` only. `CommodityChip` no longer renders a visible `{confidence}%` (was `size="md"` only; `sm` already hid it). Chip shows ticker + direction arrow only. `confidence` stays on the prop/API for gating and for the `aria-label`, now "USOIL up, model classification confidence 88%" so screen readers do not hear a price probability. Usage sites (`SignalCard`, `SignalQuickView`, `MapSignalPopup`, events/alerts/watchlist) do not reprint a per-asset percent beside the chip. Signal-level confidence elsewhere (dashboard featured, quick-view header, map popup header, landing "Neural Confidence") left as-is. **Verified:** `pnpm --filter web test` (signal-display + CommodityChip render). **Not this commit:** #139 research, #143 classifier/prompt work, landing-page "Detection Conf" custom pills (not `CommodityChip`).
- #138 remainder (full close) — 2026-09-13 (`b864eb8`). Voice guide recorded as D24 / ADR 020. Next.js `apiErrorLogged()` stops forwarding provider `.message`. Catalog fixes: settings jargon + PostgREST toasts; auth `"Missing Supabase env vars."` (kept GoTrue actionable messages); confirm-page no longer interpolates bridge errors; dashboard/map no longer show `fallbackReason` codes; Discord/alerts/event-detail mutation toasts allowlist-only; admin service-status fixed sentence. Query-internal leftovers unchanged (TelegramConnect / CommandPalette load throws; middleware 8s timeout copy). Coming-soon copy still accurate. **Verified:** `pnpm --filter web test`. No Anthropic. **Not this commit:** #139.
- #138 Phase 2 error-copy integrity — 2026-09-13 (`0eb53a4`). `apps/web` only. Replaced raw `"Supabase client not available"` throws (alerts page two mutationFns, event-detail create-rule, DiscordConnect upsert) with `throwIfNoSupabase()` — toast/catch still shows `err.message`, but that message is now "We couldn't connect to your account right now — please refresh and try again." Technical string is `console.error` only. Path is missing `NEXT_PUBLIC_SUPABASE_*` env (not a live network drop). **IngestionStatusBanner:** `degraded` means Redis `pipeline:last_run` is missing; API `reason` named Upstash/quota internals and was interpolated. Now logs `reason` and shows fixed "Per-collector health is temporarily unavailable." No last-healthy timestamp exists — copy still assumes transience. **Middleware** login `error=` copy left as-is: it only fires on the 8s `getUser()` timeout (`timedOut`), not AuthApiError/no-session; a longer Auth outage would keep hitting the same timeout and cannot be distinguished. **TelegramConnect** / **CommandPalette** "Failed to load …" throws stay query-internal (`isError` unused; no toast/inline). **Accuracy** / **admin metrics** no longer interpolate `{error.message}`. **Coming soon (unchanged, still accurate):** WelcomeTourStep "Demo preview coming soon" only on failed `/onboarding/welcome-demo.gif` (file still missing); landing "full docs coming soon" still links `/terms`. **Verified:** `pnpm --filter web test` (signal-display + user-error-copy) + grep — technical string only in `lib/user-error-copy.ts` constant / `console.error`. No Anthropic. **Remainder of original #138:** voice guide + `api-response.ts` catalog (not this prompt). #139 still research-first, not this commit.
- #137 event-page trust/UX copy — 2026-09-13 (`46264b2`). `apps/web` only; four live-test bugs, display-logic only. **(1)** Chat history load now maps 401 / 403 `chat_early_access_only` / 5xx / no-response to `HistoryErrorCode` + `HISTORY_ERROR_COPY` (same pattern as send-path `SendErrorCode`). Early-access still uses the embedded modal; a plain 401 is handled regardless of body shape. Backend `requireUser()` not touched. **(2)** Price subtext: diagnosed against `evavcgfmemwryggdkjmx` — `/api/signals/:id` fetches `priceAtSignal` (`fetched_at <= event_date`) and `currentPrice` (latest row) independently. Signal `7b43abe7-…` USOIL/UKOIL/XAUUSD were $100.05 / $104.61 / $4408.90 at 16:30 and the same quotes on a later 19:00 tick — a real flat, not a stale-same-row bug. Subtext under ±0.05% is now "No price move recorded yet since this fired" instead of "(+0.0%)". PROJECTED IMPACT CTA uses the existing severity≥7 gate (`SeverityBadge` / `generateAnalysis`): "CREATE SEVERE ALERT" only at ≥7; below that, quieter "Create Alert". **(3)** ANALYSIS-tab "Confirmed by N source(s)" Verification box removed (founder 2026-09-12); SOURCES tab unchanged. **(4)** "restoring as intelligence capacity" removed from `events/[id]/page.tsx` and `SignalQuickView.tsx`. Below severity 7: honest severity-gated copy. Severity ≥7 with empty `ai_analysis`: separate pipeline-failure sentence. **Verified:** `apps/web` helper tests (`lib/signal-display.test.ts`) + grep — old restoring-capacity / generic history / ANALYSIS "Confirmed by" strings gone. No Anthropic spend. **Not this commit:** #138 site-wide copy pass and #139 ingestion/severity audit are queue items only.
- #53 `commodity_impacts` remainder backfill — production run 2026-09-12 (this commit, docs-only). Re-ran existing `pnpm backfill:commodity-impacts` against `evavcgfmemwryggdkjmx` after Anthropic credit restore. No application code changed. LIMIT=3: 0 filled / 3 classified empty / 0 errors. Full remainder: queued=234, filled=16, classified_empty=218, errors=0. Combined this session: 16 filled / 221 classified empty / 0 errors. **Final:** 2,891 signals, **1,678 filled / 1,213 empty** — every remaining empty is now Haiku-classified `[]` and checkpointed. Ingestion bucket $0.11→$0.27 (under the $2/day cap).
- Discord alert channel (webhook-URL-paste) — 2026-09-12 (this commit). `user_channels.discord_webhook_url` + `discord_connected_at` (migration `20260912134700_user_channels_discord.sql`, applied live to `evavcgfmemwryggdkjmx` as `user_channels_discord`). Existing `user_channels_all_own` (FOR ALL, `user_id = auth.uid()`) left untouched — it already covers the new columns; confirmed via `pg_policy`. No CHECK on `alert_rules.channels`. **Dispatcher:** `alert-dispatcher.ts` now selects `discord_webhook_url` and has a `channel === "discord"` branch matching Slack (axios POST `{ content }` truncated to 2000 chars, 10s timeout; missing URL → `queued`; throw/non-2xx → `failed`). Telegram/slack/webhook branches untouched. **Settings:** `<DiscordConnect />` after TelegramConnect (paste / Save upsert / Test via `POST /api/discord/test` / Disconnect). **Alerts modal:** Telegram / Discord / Slack checkboxes; defaults to connected channels, else `["telegram"]`. **Verified:** Playwright on the standing test account at `localhost:3000` — Settings NOTIFICATIONS Discord block (instruction + Save/Test) and the create-rule checkboxes (Telegram Connected + checked; Discord/Slack Not connected). Local `dispatchAlertsForSignal` against production with a well-formed but invalid Discord webhook: `alerts_sent` wrote `channel=discord, status=failed` and the same rule's `channel=telegram` stayed `delivered` (isolation). **Could not verify** a real Discord-channel landing / `discord_connected_at` via Save+Test — no webhook URL was available to this session. `events/[id]` still has `modalChannels` with no checkboxes (pre-existing, outside this file list).
- #133 mobile dashboard shell + #112 Telegram connect UX — 2026-09-12 (`30b5f38`). Two independent `apps/web` items that both touch `TopBar.tsx`. **#133:** dashboard shell was crushed at 390px because `(dashboard)/layout.tsx` hardcoded `marginLeft: 256px` and `Sidebar.tsx` was a fixed 256px aside with no breakpoint. Below `md`, sidebar is now an off-canvas drawer (`-translate-x-full` / `translate-x-0` + `transition-transform`), backdrop tap closes it, main content has no left margin, TopBar is `left-0` (hamburger `md:hidden` opens the drawer). At `md`+ the sidebar stays permanently visible and content stays pushed 256px — unchanged. `useUIStore.mobileSidebarOpen` (persisted with the existing store). Playwright before/after at 390 (closed / drawer open / backdrop close) plus 768 and 1440 regression shots. Leftover, not changed: `/alerts` and `/calendar` still have their own page-level `ml-[256px]` (pre-existing, outside this prompt's file list). **#112:** Telegram-only (Discord is a separate prompt). New `profiles.notification_prompt_dismissed_at timestamptz` (nullable; migration `20260912180000_profiles_notification_prompt_dismissed.sql`, applied live to `evavcgfmemwryggdkjmx` as `20260912133049`). Header `forum` icon (all sizes, distinct from the alerts bell) opens `NotificationConnectModal` wrapping the existing `<TelegramConnect />`. After 3 signal-detail mounts this session (`NOTIFICATION_PROMPT_AFTER_SIGNALS`), if the profile flag is null and `/api/telegram/status` is not connected, a compact non-blocking prompt offers the same flow; "Not now" writes `notification_prompt_dismissed_at = now()` on the user's own `profiles` row via the browser client. Settings-page `<TelegramConnect />` untouched. Closing the header modal does not set the dismissed flag.
- #111 chat quality fixes: mid-sentence cutoff + markdown rendering — 2026-09-12 (`4df4070`). Real live testing after #134's governance audit found two trust-hurting bugs: replies could cut off mid-sentence, and markdown wasn't rendered. `max_tokens` stays 600 (not raised, per instruction). **Backend** (`claude.service.ts`): new `trimToLastCompleteSentence()` helper runs only when `msg.stop_reason === "max_tokens"` — trims the raw reply back to the last `. `/`! `/`? ` boundary before it reaches `sanitizeCitedChatReply()`, so a genuinely cut-off answer reads as complete-but-shorter rather than dangling mid-word; a normal `end_turn`/`stop_sequence` finish is never trimmed, even without trailing punctuation. System prompt gains a ~180-word length instruction ("2-4 short paragraphs or a short bulleted list... unless the question genuinely needs a longer breakdown") and a markdown-formatting instruction (blank-line paragraphs, `-` bullets, `**bold**` only for key terms/numbers) — the buy/sell prohibition, personalized-advice refusal, anti-prompt-injection line, and the never-fabricate-a-URL hard rule are all untouched, word-for-word. The separate sources-*section* instruction (when to actually emit `---SOURCES---`) is strengthened to fire whenever the answer draws on the signal's stored briefing/summary/impact data, not only "if convenient." **Frontend** (`SignalChatPanel.tsx`): `CitedAssistantReply`'s inner `<div>{answer}</div>` replaced with `react-markdown`, `allowedElements={["p","strong","em","ul","ol","li"]}` — mirrors the existing pattern in `events/[id]/page.tsx`. `a`/`img` excluded on purpose: the real Sources `<ul>`/`<a>` block (untouched) stays the only surface that can render a clickable link, so a markdown link the model puts directly in its answer text can never become one. The outer `whitespace-pre-wrap` wrapper and the Sources block are otherwise byte-for-byte unchanged. **Did not touch**: any of #134's budget breakers, `CHAT_ALLOWED_EMAILS` allowlist, relevance pre-check, burst limiter, or `sanitizeCitedChatReply()` itself; the 30-msg/day cap, 2000-char input cap, or 10-message history cap. **Verified**: 4 new/updated mocked-client unit tests in `claude.service.test.ts` (no live Anthropic call, per standing restriction) — system prompt still contains all four governance phrases plus the three new instructions; a mocked `stop_reason: "max_tokens"` reply is trimmed to its last sentence exactly; a mocked `stop_reason: "end_turn"` reply with no trailing punctuation is left alone. `tsc --noEmit` clean on both `apps/backend` and `apps/web`. Frontend not browser-verified this pass (per the task's own restriction — a live chat send is founder-led after reviewing the diff).
- #111 AI usage governance + cited chat — 2026-09-12 (`d8c19ba`). Two independent UTC-day Anthropic ceilings (`ANTHROPIC_DAILY_BUDGET_USD_INGESTION` default $2, `ANTHROPIC_DAILY_BUDGET_USD_CHAT` default $2) checked inside `classifyEvent()` / `generateAnalysis()` / `chatAboutSignal()` (+ the cheap relevance Haiku). New `anthropic_daily_usage` table (migration `20260912120000_anthropic_daily_usage.sql`, applied to `evavcgfmemwryggdkjmx`; RLS on, no policies — same as `service_health_events`). Chat gated by `CHAT_ALLOWED_EMAILS` (fail closed if unset) with `403 chat_early_access_only`; existing `premium_required` plan-tier check left in place after it. Daily 30-msg cap now fails closed; added `429 rate_limited_burst` (5 / 5 min). Heuristic + Haiku relevance pre-check skips Sonnet for advice/off-topic. Replies cite only handed `raw_events` URLs (`---SOURCES---`). `[ANTHROPIC BUDGET]` logs at 50%/90%; chat 50% also emails `ADMIN_EMAILS` via existing Resend wrapper. **Step 0:** (a) new signups get `pro` — `signup/page.tsx` hardcodes `selectedPlan: "pro"` into user metadata; schema default is also `'pro'`; the free-tier chat 403 is a no-op for real users today (left unchanged). (b) the 30-msg counter previously failed open on a count-query error — confirmed and flipped to fail closed. **Verified:** mocked unit tests only — no live Anthropic calls (existing classifyEvent tests were also pinned to a throwing mock + `getClient()` refuses to construct a live SDK client when `NODE_ENV=test`). Frontend blocker/sources not browser-verified this pass (live chat send is founder-led after review). Railway backend + workers already have the three new env vars; confirm values in the dashboard (`RESEND_API_KEY` is on workers only — backend chat-budget email will no-op until that key is also on backend).
- #111/#121 architecture docs + no-mock-data audit — 2026-09-12 (this commit). Both doc trees. #111 design (grounded generation, not RAG; two-rule prompt + publishers' exclusion; #103 cross-link): `18_AI_ENGINE.md` §3b, D21/ADR 017, `03_ARCHITECTURE.md` §6. #121 methodology (48h/`event_date`, volatile/neutral out of headline, n≥20, 24h price guard quoting `1cdc95d` false-flat bug, permanent `signal_outcomes` vs 90-day `commodity_prices`; #53/#115 cross-links): `17_SIGNAL_ENGINE.md` §7, D22/ADR 018, `03_ARCHITECTURE.md` §7. Known Limitations in both `08_CURRENT_STATUS.md` (heuristic severity ids `37e6c146-…` / `5e3b9c09-…`; recurring Anthropic credit; `service_health_events` had no Claude rows until Prompt O). **Mock-data audit (no code change):** no leftover development fixtures to remove. Reported, left alone: (1) `apps/backend/src/routes/backtesting.ts:23,63` + `apps/web/app/api/backtesting/route.ts:43,78,103` — documented always-mock / `isDemo: true` demo endpoint, not leftover; (2) `apps/web/app/(dashboard)/settings/page.tsx:76-81` — "Terminal User" when profile row missing, ambiguous fallback; (3) form `placeholder=` attributes and `keepPreviousData` — not mock data; (4) prices 3-tier fallback — documented, out of scope. Welcome-tour GIF remains a known outstanding asset, not a data fixture.
- SignalChatPanel visual/layout pass + `classification_method` note + 503 `ai_temporarily_unavailable` copy — 2026-09-12 (`e27ca4c`). Same component (`SignalChatPanel.tsx`) and same backend contract. **Step 0 (real screenshots before any code, standing test account, `localhost:3000`, signal `4c8e932f-…`)**: desktop 1440 empty/error/populated were readable but small (10px heading, 9px SEND, 10px disclaimer); `text-text-secondary` / `text-muted` / `text-bg-app` do not exist in `tailwind.config.ts` so assistant bubbles inherited `--text-primary` (14.98:1 — fine by accident) and the SEND button computed `rgb(229,226,225)` on `#4EDEA3` at 1.32:1 when disabled. 768px panel was 286px (sidebar 256px + `md` 8/12 split) — placeholder truncated, SEND clipped. 390px panel was 70px — unusable. 503 intercept rendered the generic "Something went wrong sending that" catch-all, not Prompt O's error shape. **Shipped**: restyled panel (working stitch tokens `text-on-surface` / `text-on-surface-variant` / `text-surface-container-lowest`, 14–15px body, 12px disclaimer, left accent bar, Intercom-style empty state + footer disclaimer); composer stacks below `@[420px]`; heuristic note when `classificationMethod === 'heuristic'`; 503 → "BBR's AI service is temporarily unavailable — try again shortly". Event-detail BFF now maps `classification_method` onto `Signal.classificationMethod`. Event page horizontal padding `px-4` on small screens (70px → 102px at 390; remaining crush is the dashboard's fixed 256px sidebar, out of scope). **Verified**: Playwright before/after at 1440/768/390 + heuristic flag + 503 copy on the same real signal. SEND contrast 1.32 → 11.31; disclaimer 10px inherited-primary → 12px `on-surface-variant` at 10.08:1; 768 composer no longer truncates.
- Classification trust/reliability fixes (heuristic severity cap + `classification_method` + AI health logging + chat error handling) — 2026-09-12 (this commit). **Doc-integrity note first**: the task prompt cited `claude/DOUBTS_AND_NEXT_PLAN_2026-09-11.md` Parts 2.1/3/3.1 for context and two example signal IDs — that file does not exist anywhere in this repo (working tree or git history; only `claude/NEW_signal_quality_audit_2026-09-11.md` exists, and it doesn't contain that content or those IDs). Proceeded by re-deriving everything directly against the live code and `evavcgfmemwryggdkjmx` instead of the missing doc; flagging this rather than silently substituting.

  **Discovery (Step 0)**: confirmed `heuristicClassify()`'s severity keyword tiers (war/invasion/nuclear/…→9, sanction/embargo/military/opec/…→8, tariff/trade war/…→7, tension/talks/…→6) match what the prompt described. Confirmed `signal-chat.routes.ts`'s POST handler called `chatAboutSignal()` with zero try/catch. Found `isRelevantEvent()` (`lib/relevance-filter.ts`, re-exported from `gdelt-collector.ts`) — its `GEOPOLITICAL_KEYWORDS` list does overlap `heuristicClassify()`'s severity keywords (military, sanction, tariff, etc.), but it's called identically before every `classifyEvent()` call in all three live collectors (gdelt/gnews/rss) and in `reconciliation.ts`/`acled-collector.ts` — i.e. it gates entry into `classifyEvent()` uniformly regardless of whether the real-Claude or heuristic branch inside it ends up firing, not "only before real Claude" as one hypothesis suggested. No fix needed there; not the source of the severity bug. Confirmed via `information_schema` there was no existing `classification_method`-shaped column before this migration.

  **Real false positives found by direct SQL** (replacing the missing doc's citations): signal `37e6c146-4189-4b96-be45-ad01ccaea016` ("Public comment open on environment study for proposed $1.1B military radar sites in Oregon", an unrelated local infrastructure story) — severity 8, confidence 0.76 (a value only `heuristicClassify()`'s `dynamicConfidence` formula produces, confirming heuristic not Claude). Signal `5e3b9c09-99ad-4959-88e2-dcc90c2bb629` ("9/11 in the Navy: I went to war, but never got off the boat", a personal memoir) — severity 9, confidence 0.76, same confirmation.

  **Shipped**: (1) `heuristicClassify()` severity hard-capped at 6 (`Math.min(severity, 6)`), comment cites both real signal IDs above. (2) New `signals.classification_method` (`'claude'`|`'heuristic'`) + `classification_method_inferred` (boolean) columns, migration `20260912000000_signals_classification_method.sql`, applied live to `evavcgfmemwryggdkjmx`. `ClassificationResult` gained `classificationMethod`, set explicitly by both branches of `classifyEvent()`; written by every live signal-creation path (`insertOrMergeSignal()` for rss/gnews/gdelt, `acled-collector.ts`, `reconciliation.ts`, dormant `ai-classifier.ts` — the last reads it from the pre-zod-parse `result`, since the zod schema strips undeclared fields from `parsed.data`). Surfaced in `GET /v1/signals` + `/v1/signals/:id` (`select("*")`, automatic) and `/v1/signals/latest` (added to its explicit column list). One-time backfill (same migration): rows with `confidence` exactly matching one of the six 2-decimal values `dynamicConfidence` can produce (0.55/0.62/0.69/0.76/0.83/0.90) marked `'heuristic'` + `inferred=true`; everything else left `NULL` rather than guessed. Result: **1,722 rows backfilled `heuristic`/inferred, 1,124 left `NULL`** (confirmed by `SELECT classification_method, classification_method_inferred, count(*) FROM signals GROUP BY 1,2` immediately after applying). (3) `recordServiceHealth("anthropic", ...)` added to both `classifyEvent()` (real-Claude success/error) and `chatAboutSignal()` (success/no-client/final-failure) — previously Claude/Anthropic had zero rows in `service_health_events` despite being the component `AGENTS.md` already flags as degraded. (4) `POST /v1/signals/:id/chat` now wraps `chatAboutSignal()` in try/catch → `503 { "error": "ai_temporarily_unavailable" }` on any unexpected error, replacing the previous no-try/catch-at-all state. Recorded as ADR 016 / D20 in `10_DECISIONS.md` (both trees).

  **Verified live before committing** (per Step 6 of the task — ran the code locally against production rather than waiting on a Railway deploy, same pattern as #121's `outcome-tracker:once`): `tsx src/scripts/ingest-once.ts` against `evavcgfmemwryggdkjmx` — GDELT/GNews/RSS fetched 122 articles, wrote 5 new real signals (`dc65644c…`, `85f3c56c…`, `3a319720…`, `ba49e01f…`, `4c8e932f…`). All 5: `classification_method='heuristic'`, `classification_method_inferred=false`, severity 5 (≤ the new cap; none happened to hit the 6-keyword tier this run, so the cap itself wasn't exercised by this particular batch, but the code path (`Math.min(severity,6)`) was — verified separately by unit test reasoning and by the two historical example IDs above, which *would* now cap at 6 under the new code). 5 new `anthropic`/`error` rows in `service_health_events`, each a real `400 credit balance too low` with a genuine Anthropic `request_id` and measured `latency_ms` (326–496ms) — confirms Anthropic credit is still exhausted (matches `AGENTS.md`'s existing known-open item) and that a **real-Claude-success case could not be produced/verified in this environment** for that reason; only the heuristic-fallback path was live-verifiable. `npm run build` (`tsc`) and the existing `claude.service.test.ts` suite both pass unchanged.

- #121 frontend half — public `/accuracy` page + `GET /v1/accuracy` — 2026-09-11 (`d2c57fe`). `apps/backend/src/routes/accuracy.routes.ts` (registered `/v1/accuracy`, plugin-function style matching `price-history.ts`/`signal-chat.routes.ts`) — no auth required; had to add an explicit `/v1/accuracy` exemption to the global `preHandler` auth hook in `middleware/auth.middleware.ts` (same pattern already used for `/v1/prices/history-5y`), since that hook 401s every route by default and the spec's "no auth required" wasn't otherwise true. Reads only `signal_outcomes`, never recomputes live. Returns overall + per-asset `total_scored`/`correct`/`hit_rate` (null + `not_enough_history: true` below `MIN_SAMPLE_SIZE = 20` — a judgment call, easy to retune, not a hard requirement)/`avg_move_when_correct`/`sample_size_note`/`volatile_neutral_summary` (fraction of volatile/neutral predictions whose actual move exceeded `VOLATILITY_THRESHOLD_PCT = 2%`, chosen as ~4x the worker's existing 0.5% flat-noise floor, kept fully separate from hit_rate)/`date_range`. `apps/web/app/accuracy/page.tsx` — public top-level route (not under `(dashboard)`, not in `middleware.ts`'s `PROTECTED` list, so no `requireUser()` gating needed, confirmed by inspecting `middleware.ts` directly), dark-terminal styling matching `/status`. Shows overall hit rate + avg move + sample size together (never hit rate alone), a permanent non-dismissible past-performance disclaimer, the per-asset table with the not-enough-history state, the plain-language date range, and deliberately has **no** "top signals"/"best calls" list anywhere on the page (hard rule per spec). **Real bug found and fixed during verification, not caused by this task but exposed by it**: `.in("id"/"signal_id", chunk)` calls with real ~36-char UUIDs started throwing `TypeError: fetch failed` once a chunk hit ~400 items — reproduced deterministically (380 always succeeds, 400 always fails) — a URL-length limit somewhere in the request chain, not network flakiness. This silently dropped 3 of 4 `existing_signal_outcomes` lookup chunks during last session's `outcome-tracker.ts` backfill (harmless that run, since the table was empty and "missing" was already the correct fallback) and, in this endpoint, was nulling out `date_range` on every request. Fixed by dropping the chunk size from 500→200 in both `accuracy.routes.ts` (`SIGNAL_LOOKUP_CHUNK`) and `outcome-tracker.ts` (`EXISTING_OUTCOMES_CHUNK`), plus a 3-attempt retry-with-backoff as cheap insurance against genuine transient blips. **Verified**: `GET /v1/accuracy` against production `evavcgfmemwryggdkjmx` returns real numbers — overall `total_scored=2390, correct=1124, hit_rate=0.4703, avg_move_when_correct=2.49%, date_range 2026-08-09→2026-09-09`; hand-checked USOIL by direct SQL (`SELECT count(*) FILTER (...), avg(...) ... WHERE asset='USOIL'`) — `total_scored=800, correct=395 (0.49375), avg_move_when_correct=2.5916757618488923` — matches the API response exactly. EURUSD (11 scored) and USDRUB (1 scored) both correctly show `not_enough_history: true` instead of a misleading percentage. Real rendered page screenshotted via Playwright CLI (`npx playwright screenshot`, the `@playwright/mcp` server itself wasn't connected this session) against the local dev server — confirms real numbers render (47.0% hit rate, 2390 sample size, per-asset table, disclaimer, date range), no placeholder/zero values, no top-signals list.
- #121 backend half — outcome-tracker worker — production run 2026-09-11 (`1cdc95d`). New table `signal_outcomes` (migration `20260911190000_signal_outcomes.sql`, applied to `evavcgfmemwryggdkjmx`; RLS public-read `to anon, authenticated using (true)`, no write policy — service-role only, same convention as `service_health_events`/`commodity_prices` sync). New daily worker `apps/backend/src/workers/outcome-tracker.ts` (cron `0 5 * * *`, registered in `workers.ts` alongside retention/sanctions). Discovery: `commodity_impacts` asset strings match `commodity_prices.symbol` exactly for the 6 real commodities (USOIL/UKOIL/NGAS/XAUUSD/WHEAT/CORN); the two legacy pre-#87 mislabeled entries (EURUSD/USDRUB) also match by symbol string but have no price history before 2026-09-09, so those specific (signal, asset) pairs correctly skip rather than fabricate a price via clamp-to-boundary (found and fixed during verification — see below). Manual one-time trigger `pnpm --filter backend outcome-tracker:once` run against production: **before 0 rows, after 2,965 rows** (1,586 signals processed, 298 pairs skipped — 229 EURUSD + 69 USDRUB, all "no price data within 24h of event_date/checkpoint"; 0 insert errors). Split: 1,124 correct / 1,266 incorrect / 575 unscored (volatile/neutral). **Verification note:** a first pass (no max-distance guard) wrote 3,263 rows including 349 with `price_at_event == price_at_checkpoint` — legacy EURUSD/USDRUB rows silently clamped to the single earliest forex price point (weeks away) for both the event and checkpoint lookup, fabricating a false "flat" outcome. Fixed by rejecting any "closest" price point more than 24h from its target (the largest real sync gap observed in `commodity_prices` is ~18h13m); wiped and re-ran clean. Manually verified 3 real post-fix rows against the underlying `commodity_prices` points: signal `de0cd7de-…` USOIL 84.95→84.4 = -0.647% = down = predicted up → false; signal `1a6ad900-…` XAUUSD 4430→4437.3 = +0.1648% = flat = predicted up → false; signal `fd4d0428-…` USOIL 82.4→82.68 = +0.3398% = flat = predicted up → false — all three price points confirmed as the actual closest `commodity_prices` rows by direct timestamp comparison, all three percentages/directions/correctness flags arithmetically correct.
- #53 `commodity_impacts` historical backfill — production run 2026-09-11 (`8733677`). One-time script `apps/backend/src/scripts/backfill-commodity-impacts.ts` reuses `ClaudeService.classifyEvent()` (Haiku `claude-haiku-4-5-20251001`, same function the live collectors call) and writes **only** `commodity_impacts`. Live pipeline untouched. **Before:** 2,824 rows, 767 filled / 2,057 empty. **After:** 2,829 rows, 1,634 filled / 1,195 empty. Original empty set: 864 newly filled + 992 Haiku-classified as genuinely empty (checkpointed) + 201 skipped when Anthropic returned `credit balance is too low` (not written, not checkpointed). Cost quote before writes: $1.53–$2.77 (346 input tok, $1/$5 per MTok); account limits from live headers 10k RPM / 10M ITPM / 2M OTPM (Scale). Script aborts on that credit error on a re-run. Remainder completed 2026-09-12 (docs-only commit): 16 more filled, 221 classified empty, 0 errors; final 1,678 filled / 1,213 empty of 2,891. Smoke-test: Iran-ports row `bde8c96a-cf26-4b54-a4d7-a5c2eeb04527` wrote USOIL/UKOIL/NGAS; other columns unchanged.
- #111 docs catch-up (API / DB / AI-engine / components / migration checklist / open-queue line) — 2026-09-11 (this commit). Status trio was already written with the code; this pass fills the topic files the change actually touched and adds `AGENTS.md` protocol item 7 (topic→file map) so a LIVE_TODO + 09/08/14 ship cannot again leave `05_API.md` describing a world without the new route.
- #111 AI signal chat, **frontend half — now fully shipped end-to-end** — shipped 2026-09-11 (`9f2aada`). `apps/web`: new `SignalChatPanel` (`components/signals/SignalChatPanel.tsx`, file-location convention matches `SignalQuickView.tsx`), wired into the event detail page (`app/(dashboard)/events/[id]/page.tsx`) directly below the existing Full Analyst Briefing / Impact Breakdown sections — that section itself untouched. Styling reuses the page's own CSS-variable tokens (`--border-subtle`, `--accent`, `text-muted`/`text-text-primary`, Space Grotesk labels) rather than a new visual language; the loading spinner reuses the exact `material-symbols-outlined progress_activity` + `animate-spin` pattern already shipped for #108's Backtesting Lab (`LoadMoreButton.tsx` uses the same pattern elsewhere). New same-origin proxy routes `app/api/signals/[id]/chat/route.ts` (GET+POST) follow the exact auth-forwarding pattern already used by `api/telegram/connect-code/route.ts` — resolve the caller's Supabase session server-side via `createClient()`, forward the access token as `Authorization: Bearer` to `process.env.API_URL`, pass the backend's status/body straight through (so `403 premium_required` / `429 rate_limited` reach the panel unchanged). Panel: fetches history on mount (empty state "Ask a question about this signal to get started"), optimistic user-message append + rollback on send failure, plain-language error copy for the 403/429 cases, and an always-visible (non-dismissible) disclaimer line under the input. **Live-verified with Playwright** on the standing test account against real signal `096759c2-5d38-4f8a-b2ab-81fcfcbfc168`: empty state rendered before any messages, a real question ("Why does Perim Island matter for oil shipping specifically?") got a grounded reply rendered in the panel, the disclaimer was visible throughout, and — critically — reloading the page showed the same question+reply still there (proves it reads from `signal_chat_messages` via the backend, not local component state). Test messages deleted from `evavcgfmemwryggdkjmx` after verification. **Found and fixed an unrelated environment blocker during verification**: the local Next.js 16 Turbopack dev server would not hydrate at all when driven via `127.0.0.1` (Chromium's `Origin: http://127.0.0.1:3000` header on the HMR WebSocket upgrade gets treated as cross-origin and rejected, which stalls the React debug channel and hydration never runs — a known Next 16 dev-mode issue, not an app bug); using `localhost` instead of `127.0.0.1` for local Playwright runs resolves it. No `next.config.ts` change made — noting this here for the next session that hits it.
- #111 AI signal chat, **backend half only** — shipped 2026-09-11 (`dcdc877`). `apps/backend`: `GET/POST /v1/signals/:id/chat` (`signal-chat.routes.ts`, registered in `app.ts`); `ClaudeService.chatAboutSignal()` reuses the exact `claude-sonnet-5` model + buy/sell/position-sizing/entry-exit prohibition wording from `generateAnalysis()` (copied verbatim, not rewritten), plus a chat-specific rule that recognizes and declines personalized position/portfolio-advice questions with a fixed redirect instead of answering; grounded only in that signal's own `title/summary/ai_analysis/region/country/severity/confidence/commodity_impacts/currency_pair_impacts/sources_count/event_date`. New table `signal_chat_messages` (migration `20260911180000_signal_chat_messages.sql`, applied to `evavcgfmemwryggdkjmx`), same user-owns-their-rows RLS convention as `alert_rules`/`watchlist_entries`/`saved_signals`. POST gates on `planTier !== "free"` (`403 { error: "premium_required" }`) and a custom 30-msg/24h per-user counter (`429 { error: "rate_limited" }`, counts `signal_chat_messages` rows directly — no rate-limit library exists in this app yet). Live-verified on standing test account romantannison (`pro` tier) against real signal `096759c2-5d38-4f8a-b2ab-81fcfcbfc168` ("Yemen Perim Island..."): a normal question ("Why does Perim Island matter for oil shipping specifically?") returned a grounded answer citing that signal's actual 82%/80% commodity-impact confidence figures; "I hold 200 barrels of WTI, should I add more?" was declined verbatim with "I can explain what this event means, but I can't advise on your own position — that's outside what this tool does," not answered. All 4 turns (2 user, 2 assistant) confirmed written to `signal_chat_messages` via direct SQL (ids `86372408-…`, `ffa2a819-…`, `86327480-…`, `4a0659bd-…`), then deleted (test data, not left in prod). **Frontend chat panel on the event page is NOT part of this — still open**, see `09_BACKLOG.md` #111.
- Stale Supabase project-ref citations — purged 2026-09-11 (this commit). Repo grep found the unused ref only in markdown (no code/config). Copy-paste URLs in archived `CLAUDE_CONTEXT.md` now use `evavcgfmemwryggdkjmx`; remaining historical mentions dropped so the live ref is not relabeled as wrong.
- #106 5-year historical commodity charts — shipped 2026-09-11 (`79c77b4`). On-demand `GET /v1/prices/history-5y/:symbol` calls yahoo-finance2 `chart()` (`period1` = 5y ago, `interval: "1wk"`), in-memory 15-minute cache per symbol, not stored and not scheduled (`price-syncer.ts` untouched). Unknown symbols never reach Yahoo. Watchlist drill-down (`/watchlist/[symbol]`) adds a second panel labeled "5-year history" via `/api/prices/history-5y`. Verified live: XAUUSD (GC=F) 262 weekly bars 2021-09-13→2026-09-11, first close $1749.40 / last $4387.90; Mar 2022 ~$1983 and Sep 2024 ~$2494 match public gold levels; USOIL Mar 2022 weekly close $109.33 (invasion spike); Playwright on standing test account rendered the Gold page chart (Y $1050–$5250, X Sep 2021–Sep 2026). Incomplete Yahoo series shows available history rather than erroring the panel.
- Canonical `docs/claude_project/` catch-up — 2026-09-11 (docs-only, this commit). `09_BACKLOG.md` now lists #105/#107/#108/#116/#119/#120/#122/#123/#124/#125/#126/#127 as shipped (S1/C3 strikethrough convention) and the rest of #104–#128 as still open. `08_CURRENT_STATUS.md` + `14_CHANGELOG.md` PHASE 11 summarize tonight's batch and point here for per-commit evidence. `AGENTS.md` protocol item 6: a `LIVE_TODO.md` line is not enough — the canonical 09/08/14 files must be updated in the same commit going forward.
- #126 Trust/freshness signals — shipped 2026-09-11. `Fresh Xm` tag on feed cards (featured / secondary / stream), `SignalCard`, map popup, and map list, from `signals.created_at` via existing `safeFormatDistanceToNow` (compact). Coverage line on Intelligence Feed: live distinct outlets last 24h + 13 configured RSS feeds (`RSS_FEEDS.length`) + GNews/GDELT/ACLED. Outlet field is `raw_events.raw_data.source` (RSS string / GNews `.name`) or `raw_data.domain` (GDELT); `signals` has no source/outlet column. Same-session check: `/api/signals/source-stats` returned `sourcesLast24h: 41, rssFeedCount: 13`; matching Supabase SQL on `evavcgfmemwryggdkjmx` returned `distinct_outlets: 41` (39 signals in the window).
- #127 Economic calendar filters (calendar half) — shipped 2026-09-11. Importance / country / category / timezone on `/calendar`. Did not mount `FilterBar` (feed-shaped value); reused `SELECT_CLASSES` + FilterBar-style timezone buttons. JSON times are UTC; local is display-only conversion. Filter helper vs JSON: 10 events → United States 5, medium 1, Monetary policy 6, low 0. Map chokepoint/pipeline layers still gated.
- #122 Quick-view slide-over — shipped 2026-09-11. Intelligence Feed stream rows get a desktop-only preview icon before the existing `>`. Click opens a right-half slide-over (title, severity, confidence, commodity impacts, short `aiAnalysis` excerpt from `/api/signals`; no new API fields). Closes on overlay click or X. Discoverability: added a Joyride step targeting the new icon (reuses `product_tour_completed`, no new column) — smaller than a one-time tooltip + migration. Playwright on romantannison: panel matched the El Niño row (severity 7, 85% confidence, WHEAT/CORN/NGAS/USOIL impacts, briefing excerpt); overlay and X both close it.
- #123 New tab only on quick-view "View full details" — shipped 2026-09-11 with #122. Feed row, Map popup, and Alerts signal-stream links left same-tab. The panel's "View full details" is a real `<a target="_blank" rel="noopener noreferrer">`. Playwright: row click stayed one tab at `/events/{id}`; the panel link opened a new already-logged-in event tab while the original stayed on `/dashboard`.
- #119 Onboarding GIF/video step — shipped 2026-09-11. Welcome modal before the existing 6 Joyride steps (`WelcomeTourStep`, `tourPhase: "welcome"`). Placeholder `/onboarding/welcome-demo.gif` (`<img>` for gif, looping muted autoplay `<video>` for mp4). Caption "See how it works"; "Got it, show me around" advances into the tour; "Skip tour" still persists `product_tour_completed`. Type-check only for the UI (no Playwright in `apps/web`). Real GIF still needs recording.
- #120 Plain-language AI-writing rewrite — shipped 2026-09-11. `generateAnalysis()` system prompt adds plain-language / 4-part structure / keep-hedging instructions alongside the #103 buy/sell prohibition (not replacing it). `classifyEvent()` (Haiku) untouched. Regenerated 4 real severity≥7 signals (El Niño, ECB/Iran, Perim Island, Al Faw tanker) before/after; not written back to `ai_analysis`. Unit test asserts both instruction blocks.
- #116 "Authentication is temporarily unavailable" — shipped 2026-09-11 (`a561690`). Root cause was the 3s AbortController on middleware `getUser()` (Vercel `AbortError` on `/middleware` + GoTrue `context canceled` / `dial tcp [::1]:5432: operation was canceled` on 2026-09-09; a real `/user` also succeeded in 6.4s). Timeout raised to 8s; race against a timer instead of aborting Auth's fetch; `catch` now `console.error`s name/message/code. Fail-closed redirect + login banner on timeout unchanged. Type-check only — no middleware tests exist; live Auth slowness not reproduced.
- #124 Feed filter bar + shared FilterBar — shipped 2026-09-11 (`74b815b`). Commodity/Region/min-severity/Time range (incl. 30d) on feed and map; region match is casing/hyphen insensitive. Playwright on romantannison: unfiltered 2823 → USOIL 478; map FilterBar present, This month sent `window=30d`, Africa region 20→6. Follow-up: map `fetchFiltered` now sends `limit=500` (API cap raised to match) so All / This month are not stuck on the first 20.
- #125 Trader-role saved views — shipped 2026-09-11 (`74b815b`). Feed-only Oil/Grain/Metals chips set FilterBar controls (no new schema). Playwright: Oil Desk 185 = manual Energy + Middle East; Grain `cat:agriculture`/region All 87; Metals XAUUSD 256.
- #107 follow-up Watchlist prefs-aware seed + user_preferences persist — shipped 2026-09-11 (`75d932c`). First-visit seed uses onboarding commodities ∪ forex when present (generic 8 only as fallback), still labeled “Suggested for you — remove anything you don't need.” List upserts to `user_preferences.watchlist_symbols` / `watchlist_suggested` (same `onConflict: "user_id"` path as #81); `bbr.watchlist.v1` stays a cache. Verified on standing test account romantannison: `commodities=['COPPER']` (set for this check — was empty) seeded the Copper card, not the generic 8, with the suggested banner; DB wrote `watchlist_symbols=['COPPER']`; `localStorage.clear()` + reload still showed Copper from the server row.
- #105 Map click-modal UI fix — shipped 2026-09-11 (`f6be851`)
- #107 Watchlist default-populated cards + one-click add — shipped 2026-09-11 (`f6be851`)
- #108 Backtesting Lab auto-apply filters + loading state — shipped 2026-09-11 (`f6be851`)
- #75 Vercel Fluid/SSE retirement — ac356ed
- #76 Telegram bot token + linking — re-verified 2026-09-06
- #79 Real usage/analytics tracking — 99975cc; ADMIN_EMAILS follow-up closed
  2026-09-07, live-verified on both Railway and Vercel (HTTP 200 + real data on
  /admin/metrics and /v1/admin/metrics)
- #80 Unsourced TAM figures fixed — 57a16b1
- #88 Telegram webhook secret-token hardening — 3dd675b
- #100 Vercel API_URL misconfiguration — fixed 2026-09-06
- #93 richfeed — FULLY RESOLVED 2026-09-07. Founder confirmed richfeed is his
  own separate, India-focused social-distribution project, not part of BBR's
  engineering scope. The Social Auto-Poster spec (docs 34–37 references) itself
  will not be picked up by BBR going forward — both because it's now a separate
  founder-owned project and because its own blockers (Meta Advanced Access,
  TikTok Content Posting API audit, Pinterest Standard Access) fall under the new
  permission-avoidance policy (ADR 014 / D18). No further BBR engineering
  follow-up on either.
- #94 Cost/waste "do now" batch — 59ccf2b, all 7 items closed
- #81 Personalization core (user_preferences extended, onboarding capture, feed
  filter) — 517f796, e4bcaf6, 817fdee
- #89 Watchlist preference-aware defaults — 598678e
- #82 Alerts rework + trust/differentiation — c0698fc (in-app card: Event → Why
  it matters → Which instruments → Alert threshold; prominent "alert only above
  this threshold" control; per-card "Built from" source links; one-time
  not-financial-advice line) + a269547 (same four-section reframe in the
  Telegram/Slack template). Verified 2026-09-07: Playwright screenshot of the
  reworked card against the test account + 3 real Telegram messages from a live
  dispatchAlertsForSignal() run (all delivered:1), identical structure in both,
  ai_analysis-present and ai_analysis-null branches both exercised. Threshold
  control DB round-trip confirmed (6→8).
- #83 Personalized daily digest — bc8f4e0 + 57516b0 (from-address). Migration
  adds user_preferences.digest_enabled (default true). digest-sender.ts selects
  each onboarded, digest-enabled user's own top-5 signals from the last 24h
  matched to their watched commodities/regions (no global fallback), same
  four-section email framing + trust line, scheduled via node-cron (DIGEST_CRON,
  default 06:00 UTC). Settings → Notifications opt-out toggle wired to
  digest_enabled (true→false→true round-trip confirmed). Verified 2026-09-07:
  SQL check — with test user prefs regions=[africa]/commodities=[CORN,WHEAT] the
  digest pulled 5 signals ALL matching CORN/WHEAT, only 1 of which was in the
  unfiltered global top-5 (Iran-dominated) → genuinely personalized. One real
  digest email delivered to romantannison@gmail.com via the existing Resend
  account (id e06df2b3-ea1d-410d-ac01-019fbbb678dc, status delivered).
  PROD CRON PATH — FULLY END-TO-END CONFIRMED 2026-09-07: founder added
  `RESEND_API_KEY` to the Railway `workers` service; then a one-off prod
  verification ran the real cron path (no code change): test account
  romantannison given `user_preferences.regions = ['middle-east']`, `DIGEST_CRON`
  on the `workers` service temporarily set to `15 3 * * *`, the deployed worker's
  own `cron.schedule` callback fired `runDigestOnce()` at 03:15:08 UTC →
  `[digest] sent to romantannison@gmail.com (5 signals) id=ffc24290-8ad1-4338-ac15-9c24707f60a1`,
  `digest-sender complete`. Resend confirms email `ffc24290-8ad1-4338-ac15-9c24707f60a1`
  (from `digest@send.bluebeaconresearch.com`, SES message-id, **status delivered**,
  created 03:15:02 UTC) — a new, distinct id from the earlier manual test send
  `e06df2b3-ea1d-410d-ac01-019fbbb678dc`. All 5 signals were Middle East, matching
  the test account's region pref → personalization path confirmed through the
  deployed worker. `DIGEST_CRON` reset to `0 6 * * *` afterwards; romantannison's
  region pref left in place (founder's own test account — keeps the daily cron
  exercising a real recipient). Resend still shows only the one pre-existing API
  key — same key reused for the digest's HTTP calls, which is fine.
- #86 Economic calendar — 30cf1f2. New `/calendar` page (this week + upcoming
  tables, 🔴/🟡/🟢 impact indicators, live countdown to the next high-impact
  event) backed by a **static, manually-curated** `apps/web/data/economic-calendar.json`
  — a deliberate v1 choice, not a gap: no new paid API/vendor credential before
  there's real usage to justify it. Dates pulled from each institution's own
  published schedule as of 2026-09-07 (federalreserve.gov, ecb.europa.eu,
  boj.or.jp, bls.gov, bea.gov, opec.org) — 10 events, Sept 10 → Oct 30 2026 (ECB
  x2, FOMC x2, BOJ x2, US NFP, US CPI, US GDP Q3 advance, OPEC Monthly Oil
  Market Report). Window runs ~7.5 weeks, slightly past the nominal 4-6 to avoid
  cutting the next FOMC/BOJ/ECB/GDP cluster in half. No OPEC+ ministerial
  production-quota meeting date has been published yet for this window, so only
  the confirmed Monthly Report date is listed — not guessed. Forecast/Previous/
  Actual are `null` → rendered as "—", never fabricated. Swapping to a live
  provider (e.g. Trading Economics) later is a data-source change to one JSON
  file, not a rebuild. Verified live via Playwright: real dates render, "This
  Week" correctly isolates just the Sept 10 ECB decision (today is Mon Sept 7),
  everything else falls into "Upcoming", and the countdown ticks down correctly
  against the real system clock (confirmed two reads 13s apart: 3d 09h 44m 04s
  → 3d 09h 43m 51s).

- #87 Forex pair taxonomy — **phase 1 of 3 (schema + classifier + price sync)
  CLOSED, verified 2026-09-09**, commit a15e2fd. Backend-only, no apps/web changes.
  - Migration `20260909035949_forex_pair_impacts.sql`: additive
    `signals.currency_pair_impacts jsonb not null default '[]'`. Nothing else
    touched; historical `commodity_impacts` rows with EURUSD/USDRUB left as-is.
  - `claude.service.ts`: new `ALLOWED_FOREX_PAIRS`
    (EURUSD|GBPUSD|USDJPY|USDCHF|USDRUB|USDCNY); EURUSD/USDRUB **removed** from
    `ALLOWED_COMMODITY_ASSETS` (the pre-existing mislabeling — other 6 commodity
    entries untouched); new `FOREX_PAIR_ALIASES` + `normalizeForexPair()` +
    `sanitizeForexImpacts()` mirroring the commodity equivalents; Claude prompt
    schema gains a `currencyPairImpacts` field; `ClassificationResult` gains
    `currencyPairImpacts`. Heuristic fallback: evidence-only USDRUB (Russia +
    sanctions/SWIFT/asset-freeze/price-cap regex) and USDCNY (China +
    tariff/trade-war/export-control/Taiwan regex); EURUSD/GBPUSD/USDJPY/USDCHF
    left **AI-only** on the heuristic path — no confident non-AI regex for
    Fed/ECB/BOJ/SNB events that wouldn't misfire.
  - `ai-classifier.ts`: `currencyPairImpacts` added to the zod schema and
    inserted into `signals.currency_pair_impacts`.
  - `price-syncer.ts`: new `FOREX_SYMBOLS` map (`<PAIR>=X` Yahoo format), synced
    in the same loop into the same `commodity_prices` table. All 6 tickers —
    incl. USDRUB=X / USDCNY=X — verified against a real `yf.quote()` call.
  - Verified: (1) information_schema shows the new column; (2) a real Claude
    classification of an EU/US-sanctions-on-Russia event returned USDRUB in
    `currencyPairImpacts` and **not** in `commodityImpacts`, and a real
    `signals` insert accepted it; (3) forcing `heuristicClassify()` on
    Russia/sanctions text returned a USDRUB entry (China/tariff text → USDCNY;
    neutral text → `[]`); (4) `runPriceSyncOnce()` wrote real non-null prices
    for all 6 forex symbols (EURUSD 1.163, GBPUSD 1.354, USDJPY 153.46,
    USDCHF 0.809, USDRUB 85.69, USDCNY 6.71).
  - ~~⚠️ Follow-up for phase 3: only the dormant `ai-classifier.ts` was wired;
    the live signal-creation paths still only write `commodity_impacts`.~~
    **CLOSED by phase 1B (commit abb2004), 2026-09-09.**

- #87 Forex pair taxonomy — **phase 1B (close the live-ingestion gap) CLOSED,
  verified 2026-09-09**, commit `abb2004`. Backend-only.
  - Added `currency_pair_impacts: classification.currencyPairImpacts ?? []` to
    the three real signal-creation inserts: `signal-merge.ts` `insertOrMergeSignal()`
    (the live rss/gnews/gdelt path per ADR 006), `reconciliation.ts`
    (orphan-recovery), `acled-collector.ts`. All three classify via
    `ClaudeService.classifyEvent()` → `classification` is `ClassificationResult`
    (already has `currencyPairImpacts` from phase 1); no narrower local type, no
    `any`. `ai-classifier.ts` untouched (already correct, still dormant).
  - Merge semantics (ADR 010): `insertOrMergeSignal()` writes impacts **once**,
    at signal creation — the duplicate and escalation branches never rewrite
    `commodity_impacts`, so `currency_pair_impacts` is left alone there too
    (exact parity; documented with a comment in the file).
  - Verified: a real Russia-sanctions classification pushed through the live
    `insertOrMergeSignal()` new-signal branch → stored `signals` row has
    `currency_pair_impacts` = `[{USDRUB…},{EURUSD…}]` **and** unchanged
    `commodity_impacts`; the row is matched by the same PostgREST `cs`
    (jsonb-contains) operator production commodity matching uses
    (`cs [{"asset":"USDRUB"}]` → hit; `cs [{"asset":"USDCNY"}]` → correctly no
    hit). `pnpm test` (backend) + `pnpm type-check` pass — commodity behavior
    unchanged.
  - Still pending: phase 2 (onboarding/watchlist UI), phase 3
    (alert_rules/dispatcher/digest forex matching + a real forex Telegram alert).

- #87 Forex pair taxonomy — **phase 2 of 3 (onboarding, feed filter, watchlist)
  CLOSED, verified 2026-09-09**, commit `55df380`. `apps/web` +
  `packages/shared` only. Wires the until-now-unused
  `user_preferences.forex_pairs` column into the existing #81/#89 mechanisms —
  no new onboarding path, no new feed-filter param, no separate watchlist toggle.
  - `packages/shared`: new `FOREX_PAIRS` constant (the same 6 the classifier
    emits — EURUSD/GBPUSD/USDJPY/USDCHF/USDRUB/USDCNY — slash-formatted labels,
    `unit`/`category` mirroring `COMMODITIES`); `Signal` gains optional
    `currencyPairImpacts?: CommodityImpact[]`.
  - `/onboarding` step 2: a "Currency pairs you follow" chip list reusing the
    same `chipStyle()`/`toggle()` helpers; `forex_pairs` added to the
    `user_preferences` upsert. **Fixed in passing:** that upsert lacked
    `onConflict: "user_id"` (PK is `id`, UNIQUE on `user_id`), so it 409'd and
    silently dropped *all* captured prefs for any user who already had a row;
    now names the constraint and throws on error instead of swallowing it.
  - `/api/signals?personalized=true`: the same `orParts` OR filter now also
    appends `currency_pair_impacts.cs.[{"asset":"<SYM>"}]` per preferred pair
    (one combined OR); `currencyPairImpacts` added to the row→`Signal` mapping.
  - `/watchlist`: `useMyPreferences()` returns `forexPairs`; the
    "My Commodities"↔"Show All" default seeds from `commodities ∪ forex_pairs`;
    all asset lookups run off `[...COMMODITIES, ...FOREX_PAIRS]`. `/api/prices`
    Tier-2 fallback `SYMBOLS` widened with the 6 pairs (Tier 1 already returned
    them).
  - Verified (Playwright + SQL): real onboarding wrote
    `forex_pairs=["EURUSD","USDCHF"]`; `?personalized=true&window=all` with only
    those two prefs returned exactly the one signal with a matching
    `currency_pair_impacts` and `?personalized=false` restored the full feed;
    `/watchlist` seeded EUR/USD + USD/CHF cards with live prices and the toggle
    round-tripped (13 assets under "Show All").
  - ~~Known limitation, out of scope: `/watchlist/[symbol]` drill-down still keys
    off `COMMODITIES`/`?commodity=` — a forex card links to a degraded drill-down
    (raw label, no matched signals).~~ **CLOSED in the phase-4 follow-up below.**
  - Still pending: phase 3 (alert_rules/dispatcher/digest forex matching + a real
    forex Telegram alert).

- #87 Forex pair taxonomy — **phase 3 of 3 (alert rules, dispatcher, digest)
  CLOSED, verified 2026-09-09**, commit `a102e68`. `apps/backend` + `apps/web` +
  one additive migration. Wires `forex_pairs` through the alert path the same way
  `commodities` already runs.
  - Migration `20260909044602_alert_rules_forex_pairs.sql`: additive
    `alert_rules.forex_pairs text[] not null default '{}'`, mirroring
    `alert_rules.commodities`. `min_severity` default untouched. Remote recorded
    the migration under the MCP's own timestamp `20260909044602`; local filename
    renamed to match (no slot mismatch).
  - `alert-dispatcher.ts`: instrument filter is now an OR of `commodities` and
    `forex_pairs` (a rule matches if the signal's `commodity_impacts` OR
    `currency_pair_impacts` overlap; a rule with neither list is still not
    instrument-filtered). `buildAlertBody()` "Which instruments" concatenates
    `currency_pair_impacts` after `commodity_impacts` — Telegram/Slack/in-app.
  - `digest-sender.ts`: `pref.forex_pairs` folds into the same combined
    containment `OR`; `currency_pair_impacts` added to the signal select +
    `SignalRow`; `whichWatchMatched()` credits forex hits; the "only forex_pairs
    set" case no longer early-returns; copy → "the regions, commodities, and
    forex pairs you follow"; `runDigestOnce()` prefs select gained `forex_pairs`.
  - `apps/web`: create-rule modal on `/alerts` + `/events/[id]` gained a 6-pair
    multi-select (persists `forex_pairs`); rule cards show a "Forex:" line;
    Alerts "Which instruments" renders `currencyPairImpacts`; `/api/alerts/recent`
    joins `currency_pair_impacts`.
  - Verified per doc 53's delivery standard: a forex-only rule (`regions=[]`,
    `commodities=[]`, `forex_pairs=["EURUSD"]`, sev 8) dispatched against live
    signal `4b96add1…` → `{attempted:1, delivered:1}`, one `alerts_sent` row, a
    **real Telegram message** with "WHICH INSTRUMENTS: … · EURUSD ↓ · USDJPY ↑ ·
    USDCHF ↑"; the two pre-existing rules were paused for the run (both
    reactivated, test rule deleted). `selectDigestSignalsForUser` with only
    `forex_pairs=["EURUSD"]` selected exactly that signal; `renderDigestEmail`
    text + HTML contain "forex pairs" and "EURUSD". Playwright: Alerts card
    renders EURUSD/USDJPY/USDCHF chips; modal multi-select round-tripped
    `["EURUSD","USDJPY"]` through the DB. (Real email send not exercised from
    local — no `RESEND_API_KEY` locally; set on Railway workers.)
  - No `equity_tickers` / `ticker_impacts` — equity stays gated (ADR 013 / D17).
  - #87 now fully shipped — all 3 phases done. ~~Carried-forward limitation:
    `/watchlist/[symbol]` drill-down still commodity-only.~~ **Closed in phase 4.**

- #87 Forex pair taxonomy — **phase 4 (watchlist drill-down forex support)
  CLOSED, verified 2026-09-09**, commit `accd468`. `apps/web` only, no
  migration. Closes the phase-2/3 carried-forward limitation: a followed forex
  pair now opens a working `/watchlist/[symbol]` drill-down instead of a
  degraded one (raw code, zero matched signals).
  - `/watchlist/[symbol]/page.tsx`: resolves `meta` against `COMMODITIES` then
    `FOREX_PAIRS` (so `EURUSD` shows "EUR/USD"); `isForex` flag; `isFollowed`
    now ORs `myPrefs.forexPairs.includes(symbol)`; the correlated-signals fetch
    sends `?forexPair=` instead of `?commodity=` for a forex symbol; the
    client-side impact lookup reads `ev.currencyPairImpacts` for a forex symbol.
  - `app/api/signals/route.ts`: new **separate** `?forexPair=` query param — a
    `.contains("currency_pair_impacts", JSON.stringify([{asset}]))` filter, the
    exact mirror of the existing `?commodity=` branch. `?commodity=` is
    untouched and still means `commodity_impacts` everywhere.
  - No `equity_tickers` / `ticker_impacts` — equity stays gated.
  - Verified (Playwright + SQL, per #87's standard): `/watchlist/EURUSD` renders
    "EUR/USD", "forex · Drill-Down", and lists test signal `4b96add1…` with an
    `EURUSD ↓` chip (its `currency_pair_impacts`); "You follow this" shows when
    `forex_pairs` contains `EURUSD`. `/watchlist/USOIL` unchanged ("WTI Crude",
    same commodity signals). API: `?forexPair=EURUSD&window=90d` → 1 signal;
    `?commodity=USOIL` → 429 (unchanged); `?forexPair=USOIL` → 0.

## #186 — full 24-page mobile/tablet live re-verification, 2026-09-24 (read-only, no code changed)

Independent Playwright audit against local dev (`acac825`), all 24 `apps/web/app` routes at 375/768/1440px, cross-referenced against `docs/stitch_mobile/`. Full report: `claude/MOBILE_AUDIT_FULL_2026-09-24.md`. **Not a "closed, verified" entry** — this pass found 2 real unfixed issues, listed below; everything else it checked (including re-verifying HANDOFF_186_PHASE9's claimed fixes: `38dfe8b` Replay Tour / tension tooltip / stream badges, `e1b0942` the 6-page phantom-260px-gap fix) came back confirmed-true, not re-logged here.

- ✅ **Closed, verified `58720a8` (2026-09-25):** `/watchlist` floating "+" overlap fixed by removing the mobile FAB entirely (desktop FAB kept) and adding an inline "+ Add Asset" button in the page header (44px touch target, matches `docs/stitch_mobile/mobile_commodity_watchlist` placement), reusing the same add-commodity-picker handler. Also collapsed the 3 overlapping commodity-picker mechanisms (My Commodities/Show All toggle, ADD COMMODITY dropdown, per-category chip rows) down to the single ADD COMMODITY dropdown — it already covered both commodities and forex. Playwright-verified at 375px: header button visible without scroll, no fixed element overlaps cards while scrolling, full add-Gold round trip confirmed, button measured 44px tall.
- 🟡 **`/status` subsystem rows crowd at 375px** — `flex items-center justify-between` with no `flex-wrap`; when a subsystem's detail text wraps to 2 lines, the status badge lands flush against it instead of clearly separated. No actual text overlap, just visually tight. Low priority.
- ✅ **Homepage headline fixed — 2026-09-25.** `apps/web/app/page.tsx:151-154`'s hero ("High-fidelity geopolitical intelligence → actionable trading signals", flagged above per HANDOFF_186_PHASE9/Stitch Parity Audit as already-rejected-but-still-live) replaced with "Geopolitical intelligence for markets that matter." Root layout meta description (`apps/web/app/layout.tsx:10`) updated to match — same string, same replace. The homepage's own page-level `metadata` export (`apps/web/app/page.tsx:9-18`) already carried an unrelated, non-stale description ("Source-first geopolitical research...") that never contained the old tagline, so it needed no change; the layout-level description is what actually matched the flagged/doc-cited string. Grep confirms zero remaining "actionable trading signals" in `apps/web`. **Verified:** local dev server, `curl` of rendered `<h1>` and `<meta name="description">` both show the new text exactly.
- ⚪ **New, minor:** tapping Replay Tour throws a console 404 for a missing `/onboarding/welcome-demo.gif`. UI degrades gracefully (shows "Demo preview coming soon" text instead of a broken image) — nothing visually broken, but the asset appears genuinely absent. Not viewport-specific.

**Addendum, same session — a second pass that actually opened every `docs/stitch_mobile/*/screen.png` and compared it directly (the pass above had leaned on the pre-existing Stitch Parity Audit artifact's summary instead). Two product-level findings worth a founder look, neither a mobile bug:**
- 🤔 **`/watchlist/[symbol]` (tested UKOIL) — correlated-signals list runs to `PAGE 1 OF 30`** (confirmed via the page's own pagination text), against the Stitch mock's implied ~12 signals/3 pages. Several visible headlines look tangentially related at best ("BRICS Backs Carbon Markets Partnership," "Global Leaders Back Dangote Refinery IPO"). Not asserting the correlation logic is wrong — oil is genuinely affected by a lot of geopolitical news — but worth a look at whether the matching threshold is too broad to be useful.
- 🤔 **New recommended build: chart-level signal markers on `/watchlist/[symbol]`.** The Stitch mock overlays dashed vertical lines on the price chart marking exactly when a correlated signal fired (legend: "DASHED LINES = GEOPOLITICAL ALERTS"). Live's chart has no such markers, despite the correlated-signals list right below it having exact timestamps to draw them from — real data, not fabrication, and directly visualizes the product's core pitch (geopolitical events → price moves). Worth floating as a 7th candidate alongside the existing 6 in the Button Parity & Feature Specs artifact.
- Also fixed a real methodology gap found mid-session: `/dashboard` uniquely uses a fixed `h-screen` + inner `overflow-y-auto` scroll pane (confirmed via `grep -rl "\bh-screen\b"` — no other route in `(dashboard)` does this), which silently defeats a naive `fullPage: true` Playwright screenshot. Any future full-page capture of `/dashboard` needs the viewport resized to the content's actual `scrollHeight` first, not just `fullPage: true`.

## Decisions confirmed 2026-09-07
1. Forex gate — softened, forex only, not equity. Desk-research-validated (see
   ADR amendment below).
2. India go-to-market — NO special treatment. Same as any other market unless a
   real signal appears later on its own.
3. Priya (SMB importer) persona — no more trader-community search time. If
   pursued opportunistically: NCBFAA (National Customs Brokers & Forwarders
   Association of America), weekly eBriefing reaching 250,000+ importers/exporters.
4. #77 (10-15 live interviews) — retired as scoped. Closed via desk research
   (Perplexity/Grok transcripts reviewed directly, plus independent web
   verification) instead of live interviews.
5. #78 (manual Stripe test) — deprioritized to last. Free-first strategy: no
   payment method added until real free-tier traction is observed.
6. #91 (distribution test) — no personal outreach; substituted with independent
   research.
7. #92 (concierge digest test) — dropped as a pre-build gate; digest builds now,
   corrected against real usage post-launch.
8. Validation checkpoint — CLEARED, on desk research plus the free-first launch
   strategy, not on live interviews or a payment test.

## Priority queue (updated 2026-09-07)
- #82, #83, and #86 closed 2026-09-07 (see "Closed, verified"). #83's prod
  cron path is now **fully end-to-end confirmed** — a one-off prod run of the
  deployed worker's own digest cron delivered a real email via the Railway
  `RESEND_API_KEY` (Resend id `ffc24290-8ad1-4338-ac15-9c24707f60a1`, delivered).
  No follow-ups outstanding.
- #87 forex taxonomy: **fully shipped 2026-09-09** — phase 1 (`a15e2fd`), phase
  1B (`abb2004`), phase 2 (`55df380`), phase 3 (alert_rules/dispatcher/digest
  forex matching + Alerts UI, `a102e68` — real forex-only Telegram alert
  verified), phase 4 (watchlist drill-down forex support, `accd468`). All
  closed — see "Closed, verified". Equity still gated.
- Gated on real free-tier traction, no fixed date: #84 (full billing), #78
- Anthropic credit topped up 2026-09-12 (~$6). Regular cron ingestion is safe
  under the new ingestion budget. #53 remainder completed 2026-09-12. Promoting
  #111 chat still waits on the founder confirming Railway `CHAT_ALLOWED_EMAILS` /
  budget values, then one manual allowlisted chat message.
- Parked: #90 (individual-stock-idea feature), #96 (Railway service merge —
  decided against)

## Priority queue update — 2026-09-10/11, founder idea batches + competitor research
(All items below are PLANS/RESEARCH ONLY, nothing here is built — do not mark any as done.)
- #186 responsive rework — re-planned 2026-09-23 around the Stitch mobile mocks (`d10626b`) as layout reference (see D29/ADR 025). **Status as of session end 2026-09-23 (paused for context handoff — see `docs/brain/HANDOFF_186_PHASE5.md`):**
  - ✅ Phase 1 — copy integrity + design-system declaration. Closed.
  - ✅ Phase 2 — `/alerts` + `/calendar` overflow (root cause: duplicate `ml-[256px] mr-[260px]`, not TopBar). Closed.
  - ✅ Phase 3 — mobile bottom tab bar, `MobileTabBar.tsx`. Closed.
  - ✅ Phase 4 — map bottom sheet, 0%→100% visible + `MapSignalPopup` bug fix. Closed.
  - ✅ Phase 5a — 4 more pages with the Phase-2 bug class (`settings`/`backtesting`/`watchlist`/`watchlist/[symbol]`), worse severity; `ALPHA` badge removed. Closed.
  - ✅ Phase 5b (tap-target sweep) — `/events/[id]` tab-clipping bug fixed (real functionality loss). Full 24-page discovery pass run 2026-09-23, then fixed in two waves: shared components (footer, `TopBar`, `PublicHeader`, 5 auth pages) and then the per-page dense controls (selects, filter chips, desk toggles, range buttons — dashboard/alerts/watchlist/calendar/watchlist-symbol/map/backtesting/settings/help/admin). Closed — see "Closed, verified" for the full breakdown. Inline text links within a sentence ("Sign up", "sign in", "Sign up again") were deliberately left alone — WCAG 2.5.5 exempts them. Driver.js's onboarding beacon and MapLibre's own attribution link were also deliberately left alone (third-party, transient/required, not real app controls).
  - ✅ **Full 24-page coverage verification, done 2026-09-23 (end of session, before handoff):** every route in `apps/web/app` checked at least once this phase — either fixed, or live/source-confirmed already safe. Three pages had never been checked at all before this pass: `/onboarding` (source-audited — same fluid-card + `flexWrap` chip pattern proven safe everywhere else in the app, could not live-render because the standing test account is already onboarded and redirects; a fresh throwaway account was **not** created for this, per the standing token-discipline rule reserving that for signup-flow testing specifically), `/help` (live-verified, 0px overflow including the feedback form at 16px inputs), `/admin/service-status` (live-verified with real loaded event-log data, 0px overflow, category tabs and source chips wrap cleanly). **Zero new bugs found in any of the three** — see the full per-page table in the handoff doc.
  - ⚪ Phase 6 — 768px tablet pass. Only `/alerts` (183px, tracked, expected) and `/calendar` (0px, clean) have actually been measured at 768; the other 22 routes have not. Not started.
  - ✅ **Phase 7 — the 3 readability regressions below, all fixed and verified 2026-09-24.** Full detail:**
    - **`/dashboard` "Recent Signal Stream" rows — headline truncated to ~8-10 characters** — **closed** (`ec3e6b4`). Root cause: all metadata (severity dot, timestamp, `FreshTag`, region) shared one horizontal line with the headline (`flex items-center`); the headline only got whatever width was left over. An attempted fix (`line-clamp-2` + a mobile-only region chip) was tried and reverted the same session it was diagnosed — verified via `getComputedStyle`/`getBoundingClientRect` that the box was genuinely 2 lines tall but only 59px wide, i.e. insufficient, not stale cache. **Real fix, shipped:** `flex-col` restructure — metadata row on line 1, headline spanning the full row width on line 2 (mobile only; `md:` reverts to the exact original single-line inline layout, confirmed via computed style at 1440px). Title box measured 68-75px → 229px wide after the fix; titles now read across up to 2 lines instead of truncating.
    - **`/alerts` rule cards — the rule's own configured name truncated to "News ..."** — **closed** (`1f44534`). Real text `"News — Middle East — Severity 6+"` now reads in full, stacked above the ACTIVE badge on mobile; `md:` single-line layout unchanged. Verified against the account's real rules, not synthetic data.
    - **`/calendar`'s event table showed Date/Time/Country by default; the event name was the 4th column, scrolled off-screen** — **closed** (`2c46df9`). Went further than a column reorder, per the Stitch mock's better-suited pattern for this specific case: built an actual mobile-only stacked-card layout (event name as heading, country/impact/date/time below, forecast grid only when data exists), desktop table kept 100% unchanged behind `hidden md:block`. All 7 upcoming events verified showing full names immediately, no scroll needed.
  - ✅ **Phase 8 (2026-09-24) — the same "technically fine, not actually good" pattern found on 3 more pages during a second, more rigorous review pass (after the founder caught the Phase 6b verdict below being too shallow — no full scroll-through, no component-by-component measurement). All fixed:**
    - **`/` (homepage) — hero/live-signal-card/features/pricing/footer all oversized for mobile** (same root cause as the dashboard bug: desktop spacing values with no mobile-specific override) — **closed** (`473ac8d`). Scroll-to-content roughly halved (~9500px → ~4956px total page height at 375px). A follow-up regression was caught and fixed in the same commit: the live-signal card's "Sign up to read the full assessment" CTA is `absolute`-overlaid on the card on desktop (a deliberate fade-teaser effect) — once the card got shorter on mobile, the overlay's fixed `top-[60%]` math landed on top of real text (the confidence caption + progress bar), a real visual collision the founder caught in a screenshot. Fixed by making the CTA flow normally in-document on mobile only; desktop's absolute-overlay behavior is untouched (confirmed via computed `position`/`top`/`padding-top` at 1440px).
    - **`/backtesting` — 6 "Popular Simulations" cards stacked full-width, ~900px of scroll before the config panel** — **closed** (`0d2796c`). Mobile gets a horizontal swipe row (`md:` 2-col grid unchanged). Verified functionality, not just layout: clicked a card and confirmed the form state actually changed (Event Type → "Naval blockade", Region → "middle-east", Commodity → "USOIL"), not just that the row scrolls.
    - **`/accuracy`'s "By asset" table — same root cause as the old `/calendar` bug, found fresh this pass** — **closed** (`5644617`). A 520px-wide table in a 311px viewport pushed the "not enough history yet" message and the entire "avg move when correct" column off-screen (EURUSD/USDRUB showed "NOT EN..."). Same stacked-card fix pattern as calendar; desktop table unchanged.
    - **`/events/[id]` headline — same oversized-heading pattern as the old homepage bug** — **closed** (`2386b69`). `text-5xl` with no mobile-specific size at all (48px on a 375px screen, 5 lines) → `text-3xl` mobile, `md:text-5xl` unchanged. The `PUBLISHED`/`VERIFICATION`/`LOCATION` grid was already correctly responsive (`sm:grid-cols-3`), no change needed there.
    - **`/dashboard` filter bar — Commodity/Region/Min-severity/Time-range were 4 stacked full-width rows, ~560px before any content** — **closed** (`ec3e6b4`, same commit as the stream-row fix). Regrouped into a 3-col grid row (Commodity/Region/Severity) with Time Range spanning full width below — 2 rows instead of 4. Scroll-to-first-card measured 899px → 697px. All 4 controls confirmed still fully functional: changed the Commodity filter live and watched the feed's own total-count re-fetch from 2949 → 503 signals, not just a cosmetic value change.
    - **`/admin/metrics` — "windows are UTC" missing a leading word** — **closed** (`7500953`), trivial copy fix found incidentally.
    - **Corrected claim from the Phase 6b entry below:** its "no changes recommended" verdict for `/`, `/backtesting`, and (transitively) the dashboard filter bar was reached without a full scroll-through or a named component-vs-mock comparison — exactly the standard [[feedback_mobile_verification_rigor]] (new memory, 2026-09-24) now requires before calling a page done. Do not repeat that shortcut.
    - **Unrelated bugs found while testing Phase 7/8, fixed in parallel/separate sessions, not part of #186's own scope:** the `[object Object]` alerts-citation bug (`0d456ad`), 4 remaining "Sentinel"/AI-tool branding instances across `alerts`/`events/[id]`/`dashboard`/`TopBar` (`3526000`, `235112e` — see the reconciliation note above this section), the MapLibre zoom control's hardcoded white background (`496bb7a`), and the completely dead "Day Mode" settings toggle, removed rather than built out for real (`ee31cd1`/`8da1be2`, ADR 027/D31).
    - **A prior "icon glyphs render as raw text" claim on `/backtesting` (this session, before the entries above) is retracted — it was a false positive.** `Element.textContent` always includes a Material Symbols icon's ligature source name (e.g. "menu") even when the icon renders correctly as a glyph; confirmed the icon in question (bottom-nav "More") renders correctly in dozens of other screenshots this session. The separate "no visible Run Simulation button" observation on the same page was checked a different way (searching rendered text for "run") and still stands — the form auto-applies on input change instead of using an explicit submit button, which is a legitimate design choice, not a bug.
    - **Recommended builds (NOT yet approved/built — founder has not confirmed scope), grounded in `21_PROJECT_BRIEFING.md`'s positioning:**
      1. Dashboard: a compact real price-impact chip per row (data already computed for the hero card) — reinforces "signals traders can act on faster than Bloomberg." Explicitly **not** a CARDS/STREAM view toggle — that's surface area for a preference nobody asked for.
      2. Alerts: a lightweight "recent matches" sparkline (real matched-signal counts) — personalized alerts are BBR's named #1 advantage over WorldMonitor per the competitor table. Explicitly **not** the mock's embedded map thumbnail per alert or its fabricated "price elasticity" stat — that's the "never compete with WorldMonitor on breadth" trap the briefing doc warns against, for decoration that doesn't make an alert more useful.
      3. Calendar: a weekly day-picker strip (pure navigation, zero new data) — directly answers the competitor table's named gap ("ForexFactory... economic calendar" as a daily-habit surface). Explicitly **not** the mock's per-event volatility forecast — BBR doesn't compute that number today, and inventing one edges toward the no-buy/sell-recommendation line.
      4. Standing exclusion, everywhere: georisk indices, implied-vol stats, "Sentinel"/"Autonomous Agent" AI branding, price-target directional calls, fake SLA/latency numbers, military-clearance copy — already covered by D29/ADR 025, reconfirmed here.
  - **Founder decision (2026-09-23): no Playwright / no new test infrastructure for this work.** Manual multi-width verification (360/390/414/768/1024/1440) per phase instead.
  - `DesignSync` (Claude Design) is authorized but unused — the mocks already exist in-repo as PNG+HTML.
  - Deferred, not in scope: `RELATED PRECEDENT EVENTS` on the event-briefing mock (needs event clustering, which doesn't exist — **correction, 2026-09-24: `/watchlist/[symbol]` already has a correlated-signals list**, simpler than the mock and honestly shows "not enough price history to measure a move" instead of fabricating a number when data is thin; the *event-detail* page's version of this, which needs cross-event clustering, remains the actually-deferred item) and reviving the unused `SignalCard.tsx`'s richer severity treatment.
- #104 Vercel Hobby→Pro upgrade — ready now, $20/mo, no gate.
- #105 Map click-modal UI fix — closed 2026-09-11 (see "Closed, verified").
- #106 5-year historical commodity charts — closed 2026-09-11 (see "Closed, verified").
- #107 Watchlist default-populated cards + one-click add — closed 2026-09-11 (see "Closed, verified"). Follow-up (prefs-aware seed + `user_preferences` persist) also closed 2026-09-11 (see "Closed, verified").
- #108 Backtesting Lab auto-apply filters + loading state — closed 2026-09-11 (see "Closed, verified").
- #109 Printable architecture/flowchart doc — scoped, own session.
- #110 Named-analyst content section — parked, gated on real evidence of demand.
- #111 AI chat on event page (premium) — **closed 2026-09-11** (backend `dcdc877` + frontend `9f2aada`; see "Closed, verified"). Grounded in that signal's own data only; same buy/sell-refusal as #103/#120 plus a personalized-position-advice refusal.
- #112 Push-notification connect UX — **closed 2026-09-12** (see "Closed, verified"). Telegram-only; dismissed state is `profiles.notification_prompt_dismissed_at`.
- #113 www/apex domain redirect — ready now, free, Vercel dashboard only.
- #114 Business continuity + legal registration — checklist ready; founder to pick a
  jurisdiction and register.
- #115 Signal-quality live-data audit — ready-to-run, read-only verification prompt exists.
- #117 Global marketing/ads compliance — researched, no blocker found; action folded into
  #102.
- #118 Premium news/AI tier — step 1 (GNews Essential) no gate; steps 2-3 gated on real
  revenue.
- #119 Onboarding GIF/video step — closed 2026-09-11 (see "Closed, verified"). Real GIF still
  needs recording.
- #120 Plain-language AI-writing rewrite (generateAnalysis() system prompt) — closed 2026-09-11
  (see "Closed, verified").
- #121 Real /accuracy page + outcome-tracker worker — **fully closed 2026-09-11**
  (see "Closed, verified", both entries): `signal_outcomes` table + daily
  `outcome-tracker.ts` worker (backfilled to 2,965 rows) + public `GET /v1/accuracy`
  + public `/accuracy` page, all live-verified with real numbers. #53 backfill
  completed 2026-09-12 (1,678 filled / 1,213 Haiku-classified empty of 2,891).
- #122 Quick-view slide-over panel — closed 2026-09-11 (see "Closed, verified").
- #123 Event pages in a new tab — remainder closed 2026-09-19 (see "Closed, verified").
  Original 2026-09-11 ship (`ef73885`) was new-tab only on quick-view "View full details".
  Remaining feed / map / alerts / watchlist / notifications / command-palette / historical
  event-detail clicks now use the same `target="_blank" rel="noopener noreferrer"` pattern.
- #124 Feed filter bar (Commodity/Region/Severity/Time range) + shared FilterBar component —
  closed 2026-09-11 (see "Closed, verified").
- #125 Trader-role saved views (Oil Desk/Grain Desk/Metals Desk) — closed 2026-09-11 (see
  "Closed, verified").
- #126 Trust/freshness signals (coverage footer + "Fresh Xm" tag) — closed 2026-09-11 (see "Closed, verified").
- #127 Economic calendar filters (importance/country/category/timezone) on the existing
  /calendar page (#86) — calendar half closed 2026-09-11 (see "Closed, verified"). Map
  chokepoint/pipeline layers still gated on a real data-vendor cost check.
- #128 Human-review trust layer — Stage 1 is a founder action (stand up the real review
  process); Stages 2-3 become a normal prompt once Stage 1 is real. Do not add "human-
  reviewed" language to the product before Stage 1 is real.
- #137 Event-page trust/UX copy (chat history errors, +0.0% subtext, Verification box,
  false "restoring capacity" briefing) — **closed 2026-09-13** (see "Closed, verified").
- #138 Site-wide copy & error-state integrity pass — **fully closed 2026-09-13**
  (see "Closed, verified").
- #140 Hide raw classifier confidence on CommodityChip — **closed 2026-09-13**
  (see "Closed, verified"). Visible `{confidence}%` next to the direction arrow
  is gone; data and aria-label remain.
- #141 Materiality gate (acts on #139's audit findings) — **closed 2026-09-13**
  (`ea34f28`, see "Closed, verified"). Post-classify "does this mean anything?"
  reject step now exists at all 5 live collector call sites; 8 new `signals` columns.
- #139 Signal ingestion, filtering, and severity/confidence logic audit — **research
  written 2026-09-13 (`fba11ac`); acted on 2026-09-13 via #141 (materiality gate,
  see "Closed, verified" above).** Findings:
  `claude/85_SIGNAL_INGESTION_FILTER_SEVERITY_AUDIT.md`. Core result: there is no
  post-classify meaning gate (junk at sev 1–4 / conf ≥0.85 still becomes a `signals`
  row); the live Haiku prompt has no rubric and does not receive the article summary;
  documented Goldstein/source-count/chokepoint severity math is not in code. #141
  closed the "no meaning gate" finding; the Goldstein/source-count/chokepoint math
  finding is not addressed by #141 and remains open if the founder wants it built.
- #142 Live watchlist database table — **closed 2026-09-13** (see "Closed, verified"). Data refresh **closed 2026-09-19** (Musk evidence + AP-hack row; 8 sourced rows). Leftover: a Trump-named `individual_social_media` row is **not** shipped — academic tweet-reaction papers exist, but inclusion is a positioning decision awaiting founder sign-off. Existing office-level `US President` row stays.

- #143 Frontend consumption of the new materiality-gate fields — **closed 2026-09-13**
  (see "Closed, verified"). Event-detail / quick-view box now reads
  `event_category` / `market_mechanism` / `is_preview` / `media_impact_entity`.
  2026-09-20 leftover (partial): event-detail also reads `novelty` /
  `source_confirmation` / `materiality_reasoning`. **#178/#179 (2026-09-21):**
  list `GET /api/signals` maps the same three; map popup shows source
  confirmation (null-hidden). Still unused in the UI: `relevance` /
  `materiality_pass`.

## #186 Phase 6b — fresh per-page Stitch-vs-live re-verification, 2026-09-24 (doc-only, no code changed)

Founder pushed back on the Phase 6 handoff's "13 pages good" verdict, correctly
suspecting it was partly inherited from a prior summary rather than freshly
checked. Re-opened all 15 `docs/stitch_mobile/*/screen.png` files and took fresh
live screenshots at 375px for every page named. Corrections to the standing
per-page verdict table (`HANDOFF_186_PHASE6.md` §4):

- **`/` (homepage) — verdict flips from good to real regression.** Hero headline
  alone fills the full 812px viewport (no CTA visible without scrolling); the
  "Active Signal" preview card repeats its own headline verbatim as an italic
  quote directly below it; the 3-step feature section and the 3 pricing cards
  each use far more vertical padding than the mock. Net: ~4x the scrolling of
  the mock to reach the same content. Not scoped/fixed yet.
- **`/pricing` — does not exist, 404.** Previous handoff wrongly listed it as a
  verified-good page; it was never actually loaded. Pricing only exists as a
  section embedded in `/`. No internal links point at `/pricing` (grepped), so
  this is a doc error, not a broken-link bug.
- **`/backtesting` — confirmed real density issue**, separate from the icon/run-
  button observation below: live stacks all 6 "popular simulations" as full-
  width cards (~900px of scroll) before reaching the config panel; mock uses a
  compact horizontal-swipe row of 2.
- `/login`, `/signup`, `/status`, `/accuracy`, `/map`, `/settings`, `/watchlist`,
  `/watchlist/[symbol]` — re-verified genuinely good with fresh screenshots
  (not just re-asserted): each fits real data in roughly the mock's footprint,
  no truncation, no fabricated stats. Confidence raised from "reviewed last
  session" to "reviewed this session."
- New minor bug found: `/admin/metrics` renders the literal string "windows are
  UTC" (missing a leading word) — `apps/web/app/admin/metrics/page.tsx:100`.
  Not fixed.
- Unconfirmed, flagged not counted: on `/backtesting` this session's sandboxed
  test browser showed no visible "Run Simulation" control and some
  `material-symbols-outlined` icons rendering as raw ligature text (e.g.
  `settings_input_component`). Network log showed zero font requests fired at
  all in this browser — likely this tool's restricted network, not a production
  bug. Needs checking in a real browser before treating as confirmed.

Next: scope and fix the `/` homepage density pass and the `/backtesting`
simulations-list density pass, alongside the existing Phase 7 three bugs
(`/dashboard`, `/alerts`, `/calendar` — still open, see Phase 6/7 handoff).
None of this session's findings are fixed yet — diagnosis only.

**⚠️ CORRECTION, 2026-09-24 (see Phase 8 above): everything in this entry is now fixed
(`/`, `/backtesting`, `/admin/metrics`), and the "good, no changes recommended" verdict
for `/accuracy` above was wrong** — its "By asset" table had the identical off-screen-
column bug as the old `/calendar` table (see Phase 8), just not caught in this pass
because the verification here was a screenshot comparison, not a full scroll-through with
DOM measurement of every component. The "Unconfirmed... icon rendering as raw text" item
is also retracted as a false positive (see Phase 8) — the "no Run button" half of that
observation stands.

## Behavioral event instrumentation — Phase 0 landed 2026-09-09 (not gated, not urgent)

Passive logging only, no scoring/ranking/UI change. Research doc
`docs/claude_project/64_*`. Three new recurring `event_type` values now write to
`public.events` via the existing `logUsageEvent` path in
`apps/web/lib/funnel-events.ts` (no migration — `public.events` has no event_type
constraint; NOT fire-once, no partial unique index):

- `signal_viewed` — a feed card (dashboard featured / secondary / stream row) or
  an Alerts-page match card is opened. metadata `{ signal_id, commodities[],
  regions[], forex_pairs[] }` (`regions` is a 1-element array — a signal has one
  region — for shape-uniformity with alert_rules). Fired via `dedupe: false`, so
  every open is a row.
- `signal_source_clicked` — the Alerts-page "Built from" (#82) source link is
  clicked. Same metadata shape. `dedupe: false`.
- `watchlist_symbol_viewed` — `/watchlist/[symbol]` drill-down mount. metadata
  `{ symbol, is_forex }`. `dedupe: "entity"` on `symbol` (collapses strict-mode
  double-mount / re-render; distinct symbols each count).

Nothing reads these yet. Data has been accumulating since **2026-09-09**. A
future Stage 2 ranking/personalization thread should treat this as the
engagement history to build on. Note: an unrelated PostHog `track("signal_viewed",
…)` with a different `{signalId, severity}` shape already fires from the
`/events/[id]` detail page — different destination (PostHog, currently dormant:
`NEXT_PUBLIC_POSTHOG_KEY` unset), different trigger point; don't conflate them.

## Killed 2026-09-07

### #85 WhatsApp alert channel — KILLED 2026-09-07, not paused.

**What was planned:** direct Meta Cloud API integration (not a BSP, to avoid
per-message markup and lock-in). Required: Meta Business Portfolio + Business
Verification (business registration/address docs submitted for Meta review,
reported 1–2+ week turnaround, sometimes stuck for weeks with no response), a
dedicated phone number never used on personal WhatsApp, and a pre-approved
utility-category message template before messaging any real user. Cost: ~₹0.136
per alert outside a 24-hour session window (free within it) — roughly $16 per
10,000 alerts. Recommended gating: Pro tier ($199+) only, given the per-message
cost. Linking flow was designed to mirror Telegram's `/connect <code>` pattern —
user messages BBR's number first, opening a 24-hr window and establishing real
opt-in, never a cold broadcast. Known risks flagged at the time: template
rejections for vague copy, Business Verification stalling with no response,
accidental number restriction from messaging non-consenting numbers.

**Why killed:** (1) Business Verification is exactly the "difficult permission"
category BBR now avoids by standing policy — see ADR 014 in
`docs/brain/10_DECISIONS.md` (D18 in `docs/claude_project/10_DECISIONS.md`).
(2) Independently, WhatsApp was already the weakest-evidenced of the three
notification channels for BBR's researched (Western-trader-weighted) audience —
Telegram/Discord dominate there; WhatsApp's edge only showed in India-specific
data, and BBR separately decided not to pursue India as a distinct go-to-market
push. Not a strong bet sacrificed for policy — a lower-confidence bet that also
fails the new gate.

**To resume later:** the draft message template, the DB schema (channel columns
mirroring `user_channels`' existing Telegram pattern), and the full cost/risk
research still exist and can be rebuilt into a fresh, self-contained prompt —
don't need to re-research the Meta requirements from zero.

## Known open technical item — RESOLVED 2026-09-07
Playwright MCP's browser profile had locked twice in this environment (orphaned
`ms-playwright-mcp/mcp-chrome-*` Chrome processes from a prior session holding
the profile's singleton lock), blocking the mandated visual-verification step
both times. Fixed by re-registering the server with `--isolated` (`claude mcp
remove playwright` / `claude mcp add playwright npx '@playwright/mcp@latest' --
--isolated`) and killing the orphaned processes so the live session could pick
up a clean profile. Confirmed working: full Playwright walkthrough of #81/#89
completed same day (fresh signup → onboarding steps 1-2 → SQL-confirmed
user_preferences row → dashboard "My Feed" toggle narrows 2622→816 and reverts
→ watchlist "My Commodities"/"Show All" toggle → drill-down "You follow this"
chip). Direct SQL/API verification remains the default per token-discipline
policy; Playwright is for visual/rendering/interaction checks specifically.
