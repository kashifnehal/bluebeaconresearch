version: "3.8"
services:
  redis:
    image: redis:7.2-alpine
    container_name: bbr_redis_local
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: ["redis-server", "--save", "", "--appendonly", "no"]

volumes:
  redis_data:
    driver: local
